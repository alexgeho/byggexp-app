# WORKLOG — что сделано, по дням

# 2026-09-20 — дневная сессия (скриншоты в чате)

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 11:33 | **Онбординг**: «Anpassa startsidan» оставался отмеченным со старого флага — версионировал ключ хранения — OTA `f8f2852e` |
| 11:20 | **Skapa uppgift**: inset-разделители как в настройках, заметки к заголовку, порядок блоков — OTA `3b80b5f3` |
| 11:17 | **Verktyg**: удаление инструмента свайпом влево (как в списке проектов) — OTA `c84bec3c` |
| 11:01 | **Создание задачи как в админке**: Prioritet + Upprepa + дефолты дат — OTA `e997233a` |
| 10:01 | **Heldag** вырезан отовсюду — OTA `5042b445` |
| 08:58 | **Онбординг**: «Anpassa startsidan» отмечался от простого открытия дровера — OTA `80e73987` |
| 08:55 | **Инструменты**: пустые списки проектов/работников (403 на `/users/role/worker` + `Promise.all`) + сканирование QR при создании — бэкенд `1c85b4f`, моб. `f90bbb2e` — OTA `80e73987` |
| 08:52 | **Бэкенд**: ProjectAdmin не видел созданный им инструмент — `530701f` (прод) |
| 08:41 | **Home**: пустая карточка Anteckningar в общем стиле — иконка по центру — OTA `3cd2996a` |
| 08:38 | **Skapa projekt**: убрал кнопку Avtal + карточку AVTAL, Ekonomi → 3 блока по 2 безрамочные строки (`FloatingField`) — OTA `e246b544` |
| 08:15–08:22 | Перевёл рабочий файл в один `WORKLOG.md` (был `TZ_TASKS.md`), добавил секцию «Очередь» |

## 15:36 · «На что она влияет, где отображается потом?» + «не сохраняется, как будто и не кликал» + «переключатели другим цветом»

**Справедливое замечание: я сделал ввод без выхода.**

- [x] **БАГ (главное)**: рапорт СОХРАНЯЛСЯ, но не возвращался. Список смен отдаёт явный набор полей (`SerializedShiftRecord`), и новых там не было — поэтому экран открывался пустым, а карточка вечно предлагала «＋ Lägg till». Теперь `hourType`, `travelKm`, `travelMinutes`, `perDiem`, `dayNote`, `ataId`, `reportedAt`, `approvedAt` едут вместе со сменой — `cf2c7c1`
- [x] **Куда это попадает (то, чего не было вовсе)**: Excel-выгрузка получила колонки Hour type / Travel km / Travel h / Per diem / Approved / Note, а колонка Duration теперь показывает часы, за которые день считается, а не только отметочные. Это и есть ответ «где отображается» — выгрузка уходит в зарплату
- [x] **Карточка смены** показывает содержимое рапорта строкой: «Övertid · 12 km · Helt» вместо безликого «Rapporterad»
- [x] **Пилюли перекрашены** с синего на чернильный `#052D50`: выбранный вариант — это состояние, а синий на экране принадлежит единственному действию «Spara»
- 📦 `b8b2c047` · OTA `b4092e51`

## 15:15 · «Не понимаю какая тут логика… если он 8:00 добавил, то они есть» + «окно вылазит ужасное» + «уменьши ещё адрес»

- [x] **Противоречие чисел.** Карточка писала «0 h» сверху и «8 h» в Manuella timmar. Оба числа правдивые: часы по отметкам = 0 (GPS не отработал), объявленные вручную = 8. Но читается как баг. Теперь в шапке — часы, за которые день СЧИТАЕТСЯ: объявленные, если объявили, иначе по отметкам. Если они расходятся, отметочные показываются отдельной строкой «Stämplat» — ничего не прячется
- [x] Объявленный ноль = «ничего не объявляли»: строка показывает «＋ Lägg till», а не оранжевый «0 h»
- [x] **Окно ручных часов** было центрированной карточкой, и цифровая клавиатура накрывала её собственные кнопки («Avbryt» обрезался). Теперь нижняя шторка, поднимается вместе с клавиатурой, действия разнесены: «Spara» — крупная, «Avbryt» и «Rensa» в подвале
- [x] Пара полей «h + min» заменена ОДНИМ десятичным полем: «7,5». «7,5» и «7.5» одинаково принимаются
- [x] **Адрес** ещё короче: оставляю улицу и город, выкидываю индекс и страну — карточке надо сказать, КАКОЙ объект, а не продиктовать почтовый адрес
- [x] 7 новых тестов (эффективные часы + короткий адрес), всего 315
- 📦 `0457087c` · OTA `3a1f59cc`

## 15:03 · «Ограничь адрес, неочевидно что можно нажать на 0m, и М убери — минуты тут никто считать не будет» (скрин Arbetspass)

- [x] **Минуты убраны везде**: длительность читается десятичными часами — «7,5 h», то самое число, что попадает в зарплату, вместо «7h 30m». Правило одно и в одном хелпере, поэтому карточка, итог месяца и ячейка календаря согласованы (в узкой ячейке единица опущена — колонка и так про часы)
- [x] **Адрес в одну строку** с обрезкой: раньше переносился на две и распирал карточку
- [x] **«＋» у кликабельных строк**: «Manuella timmar» и «Dagens rapport» показывали просто оранжевое «Lägg till», что читалось как подпись. Теперь у них плюс — доступное действие видно, а не запоминается
- 📦 `56246c88` · OTA `2a789193`

## 15:10 · «А если человек фотографировал чеки — тогда сумма расходов за сегодня»

**Твоя задача:** материалы/расходы в рапорте дня через фото чеков.

- [x] Строка «Dagens utlägg»: фотографируешь чек → он уходит в уже существующий скан (Claude vision) → подставляются поставщик и сумма → оба остаются редактируемыми (мятый чек — ровно то место, где скан ошибается) → сохранение создаёт расход на этот объект и эту дату с приложенным фото
- [x] В строке — бегущий итог расходов за день, чтобы цифра была видна пока день свежий, а не в конце месяца
- [x] Скан везде best-effort: выключен или упал — сумма просто набирается руками; не загрузился итог — рапорт всё равно работает
- [i] Отдельную сущность «расход материала за день» не заводил: чек и есть расход, а `expenses` уже связан с проектом, датой, статусом и согласованием
- 📦 `cf8374e7` · OTA `3f837d36`

## 14:55 · «Продолжай всё по списку» (ÄTA, фото, аттестация)

- [x] **Привязка дня к ÄTA**: строка появляется, только если у проекта вообще есть ÄTA — обычный договорной день остаётся коротким (Хик). Список ÄTA админский и содержит суммы, поэтому воркеру отдаётся узкий маршрут `GET /shifts/:id/ata-options` — только номер и заголовок, и только по СВОЕЙ смене
- [x] **Фото в рапорте дня** — через уже существовавшую загрузку фото на смену, счётчик рядом
- [x] **Аттестация**: у руководителя на завершённой смене строка «Attest» — тап подтверждает день, повторный снимает. Права переиспользуют тот же фильтр доступа, что и список смен, так что подтвердить чужую невидимую смену нельзя
- [x] Бэкенд: `ataId`, `approvedAt`, `approvedByUserId` на смене + `PATCH /shifts/:id/approval` — `70026a6`
- [x] 11 языков, 308 тестов зелёные
- 📦 `30082ed4` · OTA `00b85953`

## 14:40 · «Продолжай» (избранные проекты)

- [x] **Закрепление проектов**: свайп карточки ВПРАВО закрепляет — зеркальная пара к свайпу влево на удаление (как флаг в Mail). Закреплённые всплывают наверх списка и помечены звездой. Список проектов — это же то, что открывает фильтр по проекту, так что закрепление помогает и там
- [x] Хранится на устройстве, как раскладка главной, а не на аккаунте — это личная привычка, а не данные компании
- [x] `EntityListScreen` получил слот `leftAction` (свайп вправо) — теперь доступен любому списку
- [x] 4 новых юнит-теста на сортировку закреплённых, всего 308 зелёных
- 📦 `7c1359ea` · OTA `c3ca13d0`

## 14:25 · «Продолжай, да весь неиспользуемый код удаляй»

- [x] **«Kopiera <datum>»** в рапорте дня: одной кнопкой подставляет тип часа, километры, время в пути и суточные из прошлого отчётного дня. На одном объекте это изо дня в день одно и то же, а перенабор — ровно то, что люди перестают делать. Кнопка видна, только пока день не отчитан; если запрос не удался, экран просто открывается пустым (Zeigarnik — незакрытое дело давит, закрытие должно стоить один тап)
- [x] Бэкенд: `GET /shifts/last-report` — последний отчётный день воркера
- [x] **Удалён мёртвый код**: `time-report.service.js` (бил в несуществующий на бэкенде `/time-reports`) + два его экспорта, `byggexpWordmark.js` (никто не импортировал), и убраны лишние `export` у внутренних хелперов `paymentDue` и `ReminderSheet`
- [i] Прогнал knip: неиспользуемых ФАЙЛОВ больше нет (кроме `scripts/pwa-postexport.mjs` — это инструмент веб-сборки, запускается руками при деплое, оставил). Осталось ~40 неиспользуемых экспортов — часть используется внутри своего файла, часть держат тесты; чистить отдельным заходом с прогоном тестов, а не вслепую посреди рабочей сессии
- 📦 `ace5fc28` · OTA `5434a507`

## 14:10 · «Берём всё, что есть у них, только в нашем дизайне. И с учётом lawsofux.com»

**Твоя задача:** реализовать функциональность Next в нашем дизайне по законам UX.

**Разбор Next (project.next-tech.com, PWA, Version 4.0):** один быстрый рапорт за день (дата, проект, рабочий заказ/ÄTA, роль, часы) + дневник + фото + артикулы + отсутствия; копирование вчерашнего; избранные проекты; чек-листы; аттестация часов; SMS о незаполненных часах; выгрузка в зарплату. OB/övertid/traktamente/milersättning на их сайте НЕ описаны — проверил 3 страницы.

- [x] **Шаг 1 — «Dagens rapport»**: на своей завершённой смене воркер указывает тип часа (Normal / Övertid / OB — зарплата уже умеет множитель), поездку (км, время в пути, суточные) и заметку дня. Открывается с карточки смены рядом с ручными часами; карточка показывает, отчитан ли день
- [x] Бэкенд: поля на самой смене (смена И ЕСТЬ запись дня, отдельная коллекция пришлось бы джойнить обратно) + `PATCH /shifts/:id/report`, пишет только присланные поля, только своя смена — `d6fec20`
- [x] Законы UX применены осознанно: **Хик** — выбор строкой пилюль, а не выпадашкой; **Миллер** — три блока вместо стены полей; **Фиттс** — цели 44pt, кнопка сохранения в зоне большого пальца; **Якоб** — наши же карточки и inset-разделители, учиться нечему; **Постел** — «7,5» и «7.5» одинаково принимаются, пусто = 0
- [x] 11 языков
- 📦 `2f9e7d8c` · OTA `a05ce85f`

## 13:56 · «Цвета в Mitt arbete сделай как везде, не должно быть там красного» (скрин Home)

**Твоя задача:** убрать красный из карточки Mitt arbete.

- [x] Убрал `#FF3B30` и `#FF9500`: просроченные и «скоро» теперь тем же приглушённым цветом даты, что и остальные карточки главной. Срочность несёт сама формулировка («Försenad 8 aug.»), а не цвет, выбивающийся из стекла
- 📦 `33576bcd` · OTA `6593ba00`

## 13:53 · «Сделай такой же фильтр у оферт и фактур, только фильтровать частный клиент или фирма» (скрин Verktyg)

**Твоя задача:** дропдаун-фильтр по типу клиента на оферты/фактуры.

- [x] Новый общий `FilterSelector` — та же плашка с шевроном, что у фильтра по проекту, но выбор из простого списка в шторке. Триггер берёт стили ИЗ файла проектного фильтра, так что разъехаться они не могут
- [x] На оферты и фактуры добавлен фильтр **Alla / Företag / Privatpersoner** над пилюлями статусов
- [i] Как определяется тип: документы снимают имя и номер клиента, но НЕ его тип. Поэтому тип берётся из реестра клиентов — по `customerNumber` (есть у фактур), иначе по названию; ROT-фактура считается частным лицом по определению; неизвестный клиент → «фирма» (дефолт сервера)
- 📦 `9a8318d9` · OTA `b0b5cb4a`

## 13:40 · «Делай» (перевести остальные списки на общий компонент)

**Твоя задача:** все экраны-списки через один компонент.

- [x] На `EntityListScreen` переведены: **проекты, инструменты, задачи, оферты, фактуры** (клиенты и артикулы были раньше). Семь копий одной и той же оболочки → одна
- [x] В компонент добавлены слоты под то, что у экранов своё: `beforeList` (фильтр по проекту, пилюли статусов, поиск), `headerRight` (кнопка Skanna, реестры «…»), `sections` + `renderSectionHeader` (задачи сгруппированы по проектам), `listHeader` (строка «Alla projekt»), `onBack`/`onNavigateHome`/`onNavigateMenu`/`onAdd` (у проектов навигация под защитой выхода)
- [x] В каждом экране осталось только своё: данные, карточка и вызов удаления
- [x] Линт чистый, 303 юнит-теста зелёные
- 📦 `0cd84d72` · OTA `5091d56b`

## 13:26 · «Оферты и фактуры — уже две отдельные сущности, переключатель не нужен» → «Кнопка с главной ведёт на список, создание оттуда» → «Сделай один компонент, чтобы менять в одном месте»

**Твоя задача:** развести оферты и фактуры, кнопки на главной → списки, и общий компонент списка.

- [x] Убрал сегмент-переключатель Offerter/Fakturor. Теперь два маршрута `Offers` и `Invoices`, заголовок по сущности; старый `Economy` оставлен (на него ссылаются онбординг и «Mitt arbete») и по умолчанию открывает оферты
- [x] Кнопки Offert/Faktura на главной ведут в СПИСОК, а не сразу в форму — создание через «+», как у всех сущностей
- [x] Новый **`EntityListScreen`** — единственное место, где живёт раскладка списка сущности: шапка, чипы-фильтры, карточки, свайп-удаление, пустое состояние, «+» в баре. Клиенты и артикулы уже рендерятся через него, так что правка раскладки прилетает в оба сразу
- [i] Проекты / инструменты / задачи / оферты / фактуры пока на своих экранах — у них есть свои доп-контролы (Skanna, фильтр по проекту, реестры «…», группировка по проектам). Перевести их на общий компонент можно следующим заходом, скажи

## 13:20 · «Конечно, для всех делай такое удаление»

**Твоя задача:** свайп-удаление на всех списках.

- [x] Оферты и фактуры (Ekonomi) — свайп влево → «Ta bort», строка уходит после подтверждения сервером
- [x] Артикулы — то же
- [x] Задачи — то же, но только тем ролям, кто может управлять задачами; после удаления список перечитывается (задачи лежат и внутри проектов, и в личном списке — сплайсить две формы хуже, чем перезапросить)
- [x] В `task.service` и `article.service` не было метода `remove`, хотя эндпоинты на бэкенде есть — добавил
- [i] Теперь свайп-удаление единообразно на: проектах, инструментах, клиентах, оферти, фактурах, артикулах, задачах
- 📦 `a463f735` · OTA `faf81a30`

## 13:13 · «Сделай возможность удаления клиента, также смахивая» + «Не помню, что создавал оферты — откуда они взялись?»

**Твоя задача:** свайп-удаление клиента; разобраться с двумя одинаковыми офертами.

- [x] Свайп влево по карточке клиента → красная «Ta bort», та же плашка, что в проектах и инструментах; строка уходит только после подтверждения бэкендом. Добавил `clientService.remove` (эндпоинт был, метода в сервисе не было)
- [x] **Причина двух одинаковых оферт**: защита от двойного тапа держалась на стейте `saving`, а второй тап срабатывает РАНЬШЕ, чем React перерисует кнопку disabled → создавались два документа. В админке это уже чинили через `submittingRef`. Поставил такую же синхронную защиту на оферту и на фактуру, на обе кнопки (Spara utkast / Skapa & skicka)
- [i] Кто именно создал те две оферты, по коду не скажу — но одинаковая пара с одинаковой суммой и датой ровно так и получается при двойном нажатии
- 📦 `83906ebe` · OTA `c03855de`

## 13:05 · «Весь флоу бери из админки — по созданию клиента, артикула, фактуры и оферты, там всё уже точно»

**Твоя задача:** привести 4 формы к админке.

- [x] **Фактура — byggmoms**: `reverseVAT` был жёстко зашит в `"false"`, теперь берётся у клиента (как в `InvoiceForm`) и участвует в расчёте сумм. Это реальная ошибка для B2B-строительства
- [x] **Фактура — ROT**: добавил два поля, без которых вычет не заявить в Skatteverket — `rotPersonalNumber` (личный номер покупателя) и `rotProperty` (обозначение недвижимости / BRF). Раньше уходила только сумма работ
- [x] **Фактура — остальное из админки**: `ourReference` (по умолчанию = имя того, кто выставляет), `yourReference` (= контактное лицо клиента), `deliveryDate`, `paymentTerms`, `city`, `status: draft`
- [x] **Оффер**: `status: draft` + контактное лицо (роль + имя) — та же одна строка по умолчанию, что в `OfferForm`. Суммы бэкенд считает сам, поэтому их не шлём
- [x] **Артикулы**: экран открывался формой, артикулы были закопаны под ней → теперь список (как `ArticleListPage`), форма на отдельном экране `CreateArticle`, «+» в нижнем баре
- [i] `companyFooter` слать не нужно — бэкенд сам подставляет реквизиты компании (`resolveCompanyFooter`)
- 📦 `537e7a53` · OTA `92bff084`

## 12:53 · «С клиентами так же, как с проектами / работниками / инструментами — список, фильтр частные или фирмы наверху, по умолчанию все» (3 скрина)

**Твоя задача:** Klienter должен быть экраном-списком, а не формой.

- [x] Экран открывался формой создания, а сами клиенты были закопаны под ней. Теперь это список как Projekt / Anställda / Verktyg: карточки с бейджем типа (Företag / Privat), метаданные строкой (номер · email · телефон)
- [x] Фильтр наверху: **Alla** / Företag / Privatpersoner, по умолчанию Alla. Клиенты без типа считаются фирмой (так же по умолчанию на сервере)
- [x] Форма переехала на отдельный экран `CreateClient`, открывается синим «+» в нижнем баре; после сохранения возвращает в список, который перечитывается на фокусе
- [x] 11 языков
- 📦 `ce60084c` · OTA `a07f6607`

## 12:51 · «Проверь, если фирма, то блока с ROT не будет?» (скрин Ny faktura)

**Твоя задача:** проверить ROT для клиента-фирмы.

- [x] Была ошибка: блок ROT показывался ЛЮБОМУ клиенту, и `rotEnabled` мог уехать на фактуру фирмы. В админке (`InvoiceForm`) он показан только при `clientType === 'private'` и сбрасывается при смене клиента
- [x] Теперь так же: блок скрыт, если клиент не частное лицо; смена клиента на фирму гасит тумблер и сумму; в payload `rotEnabled` форсированно false для фирмы
- 📦 `9068bfde` · OTA `5a880c46`

## 12:49 · «Title заменить на Beskrivning» (скрин Ny faktura)

**Твоя задача:** подпись строки фактуры.

- [x] Экран оффера уже передавал «Beskrivning», а фактура падала в дефолт `itemTitle` = «Titel». Сделал «Beskrivning» дефолтом редактора строк — теперь совпадает на обоих экранах; неиспользуемый ключ `itemTitle` вычистил из 11 локалей
- 📦 `56bf70a4` · OTA `42b4ffce`

## 12:35 · «Заход 2 делай»

**Твоя задача:** колокольчик-напоминание на задаче и на заметке.

- [x] Общий `ReminderSheet`: выбор момента (inline-пикер на iOS, дата→время на Android) + для задачи пилюли повтора «Один раз / каждые 15 / 30 / час»
- [x] Задача: колокольчик в строке (TasksPreview). Пишет ровно тот же контракт, что попап в админке — выбранный момент становится дедлайном, интервал > 0 включает «напоминать до выполнения». Сброс сохраняет дедлайн, гасит только напоминание
- [x] Заметка: колокольчик в строке (NotesPreview) — одно напоминание, хранится в новом поле `remindAt` на сервере, а не локально, поэтому переживает перезапуск
- [x] Колокольчик синий, когда напоминание стоит
- [x] Бэкенд: `remindAt`/`remindedAt` у заметки + крон `note-reminders` (раз в минуту, чтобы «в 14:30» приходило вовремя); `remindedAt` ставится ДО отправки пуша, поэтому упавший пуш не превращается в ежеминутный спам; перенос/сброс колокольчика взводит заново — `4ea6a30`
- [x] 11 языков
- 📦 `f4243e9f` · OTA `2b172402`

## 12:15 · «Напоминание о входящих счетах и о заметках… предложи варианты» → «Если в админке Mitt arbete, то и тут так же. Действуй»

**Твоя задача:** заход 1 — счета, под именем «Mitt arbete» как в админке.

- [x] Новая секция главной **«Mitt arbete»**: неоплаченные входящие счета (срочные первыми, просроченные красным, ближайшие 7 дней оранжевым) + просроченные задачи. До 3 строк каждого
- [x] Тап по кружку у счёта = «оплачено» → статус `paid`, напоминания прекращаются
- [x] `paymentDue.js` — дословный порт из админки (`isUnpaid`/`daysUntilDue`/`paymentDueTone`, порог 7 дней): красное/оранжевое значит одно и то же в вебе и в приложении
- [x] Новый `supplier-invoice.service.js` (`GET /supplier-invoices`, `PATCH :id/status`) — в мобиле его не было
- [x] Секция ВЫКЛЮЧЕНА по умолчанию, включается в Anpassa startsidan; счета грузятся только при праве `finance.manage`
- [x] Бэкенд: крон `payment-reminders` — пуш админам за 7 дней, за 1 день, в день срока и раз в сутки пока просрочен; веха пишется в счёт, `setStatus` её сбрасывает (оплатил → тихо, переоткрыл → снова напоминает) — `f9053b9`
- [x] 11 языков
- 📦 `3269a114` · OTA `ef8b0c63`

## 12:07 · «Значок фактуры сделай по-другому — символом денег, монеты, из той же библиотеки» (скрин Home)

**Твоя задача:** иконка фактуры не отличается от оффера.

- [x] `file` → `dollar-sign`. Монет в Feather нет, денежный символ там один; альтернатива — `credit-card`, скажи если лучше
- 📦 `189218f5` · OTA `a157406a`

## 11:52 · «Сюда надо добавить ещё две кнопки — оферта и фактура, их создание; иконки из тех же коллекций, тот же принцип; также их можно добавлять и убирать с главного экрана» (скрин Home)

**Твоя задача:** две новые кнопки на главной — создание оффера и фактуры.

- [x] `offer` → экран CreateOffer, `invoice` → CreateInvoice; иконки Feather `file-text` / `file` — ровно те, что в меню у Offerter/Fakturor
- [x] По умолчанию ВЫКЛЮЧЕНЫ, включаются/выключаются в Anpassa startsidan как любая другая кнопка (drag-порядок тоже работает)
- [x] Новое поле кнопки `permission` (у этих — `finance.manage`): скрыты у того, у кого нет права, зато видны делегированному «офисному» юзеру без админ-роли
- [x] Подписи в 11 языках + цвета для colorful-темы (из существующей палитры, новых оттенков не выдумывал)
- 📦 `ed8d6043` · OTA `ff39e54d`

## 11:45 · «Именно этот юзер он не добавлял инструмент… поэтому мы не ставим» + «Про этот экран я тебе давал задачу сегодня» (2 скрина Kom igång)

**Твоя задача:** галочка ставится, только если ЭТОТ пользователь сам это сделал.

- [x] Каждый сигнал чеклиста теперь сверяется с текущим юзером: `createdByUserId` у инструментов / задач / клиентов / артикулов / офферов / фактур, `createdBy` у приглашённого сотрудника, owner-или-руководитель у проекта
- [x] Бэкенд: у инструмента не было автора — добавил `createdByUserId`, пишется при создании (`e0d9dca`)
- [x] Бэкенд: у компании не было «кто заполнил» — добавил `detailsUpdatedByUserId`, пишется при сохранении формы (`76d9a5d`); «Fyll i företagsuppgifter» отмечается только у того, кто её заполнил
- [i] У старых записей автора нет → шаг станет неотмеченным, пока юзер не сделает своё. Это и есть нужное поведение
- 📦 `450d852d` · OTA `c8e35532`

## 11:34 · «Убери этот. Описание ячейку сделай ниже/меньше» (скрин Skapa uppgift)

**Твоя задача:** убрать «Interna anteckningar», описание сделать ниже.

- [x] «Interna anteckningar» вырезаны целиком: строка, стейт `notes`, отправка поля в submit
- [x] Ячейка Beskrivning: minHeight 70 → 40, растёт по мере набора
- 📦 `deee5001` · OTA `1b5aae63`

## 11:33 · «Почему отмечены?» (скрин Kom igång, fieldwork)

**Твоя задача:** в чеклисте отмечены Lägg till verktyg и Anpassa startsidan.

- [i] «Lägg till verktyg» — честно: ты сегодня создал 4 инструмента, шаг засчитан верно
- [x] «Anpassa startsidan» — остаток старого поведения: флаг «открыл дровер» уже лежал в AsyncStorage с 08:53, фикс в 08:58 убрал только НОВЫЕ срабатывания. Версионировал ключ (`home-onboarding-customize-opened` → `home-onboarding-customized-v2`), старый флаг больше не читается и чистится при смене юзера; тест обновлён
- 📦 `a8903871` · OTA `f8f2852e`

## 11:20 · «Стиль сломался — смотри как у настроек разделительная линия. Заметки перенеси к заголовку. После идут напоминания. Тут уже приоритетность и повторение» (скрин Skapa uppgift)

**Твоя задача:** починить разделители и переставить блоки.

- [x] Разделители: строки больше не рисуют сплошной бордер во всю ширину. Карточка рисует hairline с отступом — 58 после иконки, 16 когда иконки нет. Те же линии, что в настройках/меню (`FieldRow.sepIcon/sepPlain`, `CreateTool.rowSep`)
- [x] Beskrivning + Anteckningar переехали в карточку с заголовком (описывают одно и то же)
- [x] Новый порядок: заголовок (+описание+заметки) → даты → **Aviseringar** → **Prioritet + Upprepa** → файлы
- 📦 `912a5e5a` · OTA `3b80b5f3`

## 11:17 · «Делай возможность удалять инструмент который админ сам добавил, также как и сотрудников — влево смахивая» (скрин Verktyg)

**Твоя задача:** удаление инструмента свайпом влево.

- [x] Свайп влево на карточке инструмента → красная «Ta bort» (тот же `Swipeable` + та же красная плашка, что в списке проектов). Без подтверждения — сам свайп и есть защита, как у проектов
- [x] Видно только ролям, которые управляют инструментами (superadmin / companyAdmin / projectAdmin) — бэкенд уже проверяет доступ и компанию на `DELETE /tools/:id`
- [x] Карточка пропадает только после подтверждения бэкендом; при ошибке — алерт с `[код] сообщение`
- [x] Ключи `common.delete` + `tools.deleteFailed` в 11 языках
- 📦 `d28b53e7` · OTA `c84bec3c`

## 11:01 · «дела как в админке» (2 скрина Create task из админки)

**Твоя задача:** сделать создание задачи в приложении как в админке.

- [x] Prioritet (Låg / Normal / Hög) и Upprepa (Upprepas inte / Varje dag / Varje vardag / Varje vecka / Varannan vecka / Varje månad) — отдельной карточкой, выбор через новый `OptionPickerModal`
- [x] Оба поля уходят на бэкенд — он их уже поддерживал (`priority`, `recurrence`), мобила НИКОГДА не слала → всё падало в normal/none
- [x] Даты предзаполнены: сегодня, старт 08:00, дедлайн 17:00 — дефолты админки
- [x] 11 языков
- [i] Påminnelser + «Meddela chefen efter N» в мобиле УЖЕ есть (шторка Notifications), просто разложено иначе, чем в вебе. Один-в-один выпадашка (Av/15/30/час/день) — по запросу
- 📦 `16cdb2ef` · OTA `e997233a`

## 10:01 · «Heldag убери вообще» → «Отовсюду»

**Твоя задача:** убрать блок Heldag из Skapa uppgift полностью.

- [x] Вырезал: строку-свитч, стейт `allDay` + поле в черновике, `buildAllDayRange` и мёртвые хелперы времени, стили `allDayTextContainer`/`allDayHint`, ключи `createTask.allDay*` во всех 11 локалях. Блок Schema = только Börjar / Slutar
- 📦 `787bca71` · OTA `5042b445`

## 08:53 · «Я еще не добавлял ни клиента ни фактуру ни своих данных не заполнял» + «Экран тоже не настраивал» (2 скрина чеклиста)

**Твоя задача:** в онбординге отмечены пункты, которых ты не делал.

- [x] «Anpassa startsidan» отмечался при ПРОСТОМ ОТКРЫТИИ дровера → теперь флаг ставится внутри дровера, когда реально меняешь тему/кнопку/секцию — `dc335160` · OTA `80e73987`
- [i] Остальные три (företagsuppgifter / klient / offert-faktura) — НЕ баг: все запросы строго companyId-scoped (проверил `findAccessible` у clients/offers/invoices), демо-сидов нет. Значит в компании реально лежат orgNumber + ≥1 клиент + ≥1 оффер/фактура (заведены раньше через админку). Хочешь «сделал ли это Я» вместо «есть ли у компании» — переделать сигнал на per-user (ЖДЁТ ТВОЕГО ОТВЕТА)

## 08:49–08:50 · «Создал инструмент а его и нету» + «Не показывает ни проекты ни людей» (3 скрина)

**Твоя задача:** инструмент создался, но списка нет; в «Koppla till projekt/arbetare» пусто.

- [x] ProjectAdmin в `findAccessible` видел ТОЛЬКО инструменты своих проектов, а у нового инструмента `projectIds` пуст → создатель не видел собственный инструмент. Теперь список скоупится по companyId как у CompanyAdmin (ProjectAdmin и так цепляет инструменты по всей компании) — бэкенд `530701f` (08:52, прод)
- [x] `GET /users/role/:role` = ТОЛЬКО superadmin → 403 для company/projectAdmin, и один `Promise.all` reject обнулял ЗАОДНО и проекты. Теперь `/users/my-company` + фильтр `role==='worker'`, загрузки независимы (`allSettled`) — `f90bbb2e` · OTA `80e73987`

## 08:48 · «Добавь функцию сканирования» → «У каждого инструмента свой QR-код и можно добавить просто отсканировав» (скрин Lägg till verktyg)

**Твоя задача:** перенести логику сканирования на экран создания инструмента.

- [x] Новая строка «QR-kod» → камера (переиспользуемый `QrScannerModal`, вынес из ToolScanScreen) → код сохраняется как `qrId` инструмента, потом «Skanna» по нему находит инструмент
- [x] Бэкенд принимает `qrId` на create (trim + upper-case, 409 если код занят), без кода — генерит сам как раньше — `1c85b4f`
- [x] 11 языков · OTA `80e73987`

## 08:40 · «Давай в 1м стиле все сделаем» → «Пока заметок нету так же в центр и с иконкой» (скрин Home)

**Твоя задача:** пустая карточка Anteckningar выбивается из стиля Dagsrapport/Uppgifter.

- [x] Пустой стейт по центру: иконка `edit-3` + «Skriv en anteckning…», карточка 130px как у соседей. Инпут остаётся смонтированным (свёрнут), тап разворачивает и фокусит
- 📦 `5d04f930` · OTA `3cd2996a`

## 08:36 · «Убери Avtal кнопку и из окна Ekonomi. В окне Ekonomi сделай строчками, разбей на 3 блока по 2 строчки» (3 скрина)

**Твоя задача:** убрать Avtal, Ekonomi строками, 3 блока по 2, строки как Projektnamn/Orderreferens но не обязательные.

- [x] Убрал кнопку «Avtal» на Skapa projekt + карточку AVTAL/Avtalsnr в окне Ekonomi; выпилил `ContractSection`, стейт `contractNumber` и его append в submit
- [x] Ekonomi: 2-колоночные пилюли → полноширинные СТРОКИ
- [x] 3 блока по 2 строки (budget/timmar · planerat/förbrukat material · självkostnad/debiteras)
- [x] Строки = новый `FloatingField` (floating label + hairline sep, безрамочный), без звёздочки
- [i] Заголовок «EKONOMI» внутри убрал — шапка экрана уже «Ekonomi» (дубль)
- 📦 `c3b30129` · OTA `e246b544`

## 08:15–08:22 · Организация работы

- [x] Свёл всё в один `WORKLOG.md` (был `TZ_TASKS.md`), `HANDOFF.md` помечен архивом — `54bd7e1f`
- [x] Добавил секцию «Очередь»: задачи выписываются ДО выполнения — `d2b36325`

# 2026-09-19 → 20, ночная сессия (раунды 1–5)

## Экран Projektadress (LocationPickerModal)

- [x] Кнопку сохранить — синяя галочка в шапке справа (как в Skapa projekt) — OTA
- [x] Выбор радиуса — над картой — OTA
- [x] Скрывать клавиатуру при выборе адреса / касании слайдера / карты — OTA
- [x] Слайдер радиуса не тянулся на Android (убрал сломавший wrapper, вернул прямой Slider) — OTA — ⏳ проверить перетаскивание на устройстве
- [x] Слайдер пропал → восстановил — OTA
- [x] Бегунок (thumb) не виден → белый thumb — OTA (виден на 6259)
- [x] Галочка на разной ВЫСОТЕ (модалка без safe-area) → paddingTop insets.top — OTA
- [x] Галочка РАЗНОГО ЦВЕТА на разных экранах → HeaderCheckButton теперь = theme.colors.primary (как FAB), убрал disabled-затухание на Projektadress — OTA

## Экран Skapa projekt (CreateProject)

- [x] Notes: не виден placeholder (тёмная тема) → placeholderTextColor — OTA
- [x] Notes: «чёрная полоса» = Android underline → underlineColorAndroid="transparent" — OTA
- [x] Notes: ячейку чуть меньше → minHeight 100→64 — OTA
- [x] Имя проекта: при клике авто-адрес очищается — OTA

## Меню / профиль

- [x] У АДМИНА в шапке меню «Sverige Bygg AB» → у companyAdmin имя-строка = РОЛЬ (Företagsadministratör), бейдж прячется если нет профессии — OTA
- [x] Home-счётчики «сверяются с БД» (рефетч на фокусе) — OTA
- [x] Тёмное меню (dark theme) — OTA (раунд 1)
- [x] Бейдж профессии вместо «Arbetare» у воркера — OTA (раунд 1)
- [x] Онбординг: шаг «Anpassa startsidan» админу — OTA

## Разъяснения (НЕ баги кода)

- [i] «Sverige Bygg AB» нигде не захардкожено (грепнул 3 репо) — это `user.name` аккаунта в БД
- [i] Оффер/фактура/клиент в чеклисте отмечены — запросы строго companyId-scoped; данные реально есть на аккаунте `company@byggexp.se`
- [i] «OTA не долетела до Android» — долетела (билд 26 = runtime 1.1.0/production); применяется на перезапуск
- [i] iOS геозона: realtime-push при выходе не приходит — ограничение iOS region-monitoring (foreground safety-net ловит на возврате); фикс = syslog-диагностика / больший радиус (трогает закреплённый конфиг) — открыто

## Раунд 3 (фидбэк 21:20+)

- [x] Онбординг «Bjud in ditt team» не отмечался после добавления сотрудника → `hasTeam` теперь = есть любой юзер компании кроме админа (вкл. приглашённых pending), без требования заполнять все поля — OTA
- [x] Слайдер thumb — белый, чуть выше (transform-скейл откатил: делал овал/клип); реально БОЛЬШЕ thumb = нужен `thumbImage`-ассет (могу добавить)
- [ ] Чёрная линия у notes — в КОДЕ источника нет (ни бордера, ни divider, ни глоб.дефолта); `underlineColorAndroid="transparent"` НЕ срабатывает на Android New-Arch (Fabric) для multiline; OTA с фиксом уже применён, линия осталась → нужен Android ПО КАБЕЛЮ (adb dump дерева вьюх), вслепую не найти

## Раунд 4 (adb-сессия, подтверждено на устройстве)

- [x] Чёрная линия notes — источник = нативный EditText underline (нашёл adb-дампом); фикс = сплошной backgroundColor — ✅ OTA
- [x] Слайдер радиуса **тянется на Android** — заменил @react-native-community/slider на свой `RadiusSlider` (PanResponder, сам захватывает касание) — ✅ ПОДТВЕРЖДЕНО Alexander'ом
- [x] Thumb крупный (44dp) + круговая тень (offset 0,0) — ✅
- [x] Меню: у админа имя (`user.name`) + бейдж профессии как у воркера (откат «роль вместо имени») — ✅ OTA
- [x] Ekonomi (`FieldInput`): крупнее/серая подпись + выровненные 2-колоночные пилюли — ✅ OTA
- [x] `/app/magic` fallback: кнопка + авто «Öppna appen» (deep-link), иначе стор — ✅ backend

## Раунд 5 (фидбэк 23:xx, iPhone по кабелю)

- [x] Удаление проекта: ProjectAdmin получал `[403] Access denied` → бэкенд: добавил ProjectAdmin в `@Roles` на `DELETE /projects/:id` + `assertCanDeleteProject` (ProjectAdmin удаляет только свои: owner/projectManager) — ✅ прод `89280b9` (серверный, без пересборки/OTA)
- [x] Диагностика удаления: on-screen Alert показывает `[status] message` — сработало (скриншот `[403]`). NB: `idevicesyslog` на iOS 26 мёртв (Apple сменила протокол c iOS 17+), релиз режет console.* — логи по кабелю НЕ работают, юзать on-screen диагностику
- [x] Orderreferens: перенёс в ту же карточку, что Projektnamn, строкой под ним, без рамки (floating label + hairline sep) — ✅ OTA `bd0c4e8e`
- [x] Ekonomi «цвет как на создании»: замерил реальные пиксели — лейблы уже были идентичны (#7F7F7F=#000@50%). По выбору Alexander (AskUserQuestion, выбрал ВСЕ 3): лейблы → чёрные, поля → безрамочные, заголовки EKONOMI/AVTAL → чёрные. Через новые пропсы FieldInput `borderless`/`labelStyle` (shared-компонент не сломан) — ✅ OTA `60e55866`
- [x] Anteckning: отступ снизу 28→20 = как у всех блоков (сверху/снизу одинаково) — ✅ OTA `60e55866`
- [i] Процесс: при «сделай цвет как X» — СНАЧАЛА замерять пиксели+код, не на глаз; сохранено в память `feedback_color_match_sample_pixels`

# Открытые / след. шаги

- iOS геозона realtime-push при выходе (ограничение iOS region-monitoring) — ждёт решения (больший радиус трогает закреплённый gps-конфиг; syslog по кабелю на iOS 26 недоступен без pymobiledevice3+tunnel)
- Слайдер радиуса на Android — перетаскивание подтверждено; ⏳ проверить в свежем OTA-билде что не регрессировало
- Ekonomi: те же чёрные-безрамочные стили НЕ применены к экрану деталей проекта (ProjectScreen Ekonomi) — если Alexander захочет консистентности, применить те же пропсы там (call sites ProjectScreen.jsx:706/714, ecoSectionTitle)
- Проверить на устройстве после OTA `60e55866`: Ekonomi (чёрные лейблы/заголовки, без рамок) + равные отступы Anteckning

---

# Служебное (мои правила ведения файла)

## 📥 Очередь (capture ПЕРЕД работой)

> Как только Alexander кидает задачи (текст/скриншоты) — СНАЧАЛА выписываю каждую сюда как `[ ]`, потом делаю. В конце сессии тут не должно остаться `[ ]` без пометки почему отложено.

- (пусто — список Next закрыт, очередь чиста на 2026-09-20 15:10)
  - [i] У нас УЖЕ есть и лучше: GPS-смены с авто-паузой, напоминания о незаполненных часах (пуш + эскалация), отсутствия (semester/sjuk/VAB), расходы со сканом чека, дневник, чек-листы, ÄTA, PWA
  - [i] Мёртвый `time-report.service.js` удалён (бил в несуществующий `/time-reports`)
  - [ ] Оставшийся мусор по knip: ~40 неиспользуемых ЭКСПОРТОВ (не файлов) — темы/токены, гео-константы, хелперы расписания. Часть используется внутри своего же файла и просто не должна экспортироваться, часть держат тесты. Чистить отдельным заходом с прогоном тестов, не вслепую

## Правила

> 1. Новые задачи Alexander СНАЧАЛА пишу в «Очередь» как `[ ]`, потом делаю, отмечаю `[x]`, в конце сверяюсь.
> 2. Делаю ВСЕ пункты за один заход; нерешённый — в конец, но не стопорит остальные.
> 3. У КАЖДОГО пункта — дата и время (`2026-09-20 10:01`).
> 4. Пишу ЗАДАЧУ словами Alexander'а («Твоя задача:»), а под ней — что сделано. Новое — СВЕРХУ.
> 5. У каждого дня СНАЧАЛА таблица «Время | Что» (одна строка на задачу), ниже — подробности. В таблице САМАЯ СВЕЖАЯ задача — ПЕРВОЙ СТРОКОЙ (новое сверху, как и блоки).
> 6. Служебное (правила, очередь) держу В КОНЦЕ файла — сверху только то, что сделано.

> `HANDOFF.md` = архив прошлых сессий (не веду активно). `docs/*` = релизные/тех-доки.
