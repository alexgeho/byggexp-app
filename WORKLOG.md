# WORKLOG — что сделано, по дням

**Обозначения:** `- [x]` сделано · `- [ ]` **поставлено, но НЕ сделано** (ждёт решения или следующей сессии) · `- [i]` факт/диагноз без действия. Незакрытые пункты остаются в том же дне, где их подняли, и продублированы в «Открытые / след. шаги» внизу файла.

# 2026-09-21 — дневная сессия (скриншоты в чате)

| Время | Что                                                                                                                                                             |
| ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 12:53 | **Тёмная тема**: марин-синий текст (`rgba(5,45,80,…)`) больше нигде не остаётся на тёмном фоне — 37 мест в 15 файлах переведены на токены темы — OTA `57c4904f` |
| 12:44 | **Выбор проекта**: «Alla projekt» — заголовок, а не тяжёлая плашка; карта и подсказки адреса в тёмной теме перестали светить белым — OTA `d4d861d6`             |
| 12:20 | **Кнопка «назад»**: та же поверхность, что у бара — без стекла и слоёв — OTA `c52b5439`                                                                         |
| 12:16 | **Нижний бар и «назад»** на внутренних страницах ровно как на главной — OTA `f27c0b00`                                                                          |
| 12:04 | **Пикер проекта**: только список, без поиска — выбрал и сразу назад — OTA `888243ba`                                                                            |
| 11:50 | **Удаление документа из «⋮»** (а не только свайпом), плюс копия документа — OTA `81d85e46`                                                                      |
| 11:47 | **Задачи**: группы проектов сворачиваются, заголовок группы больше не наезжает на карточки — OTA `8c74abb8`                                                     |
| 11:30 | **Пилюли статусов заметнее**, название фирмы 14 — OTA `3005e28c`                                                                                                |
| 11:17 | **Один и тот же зазор 12** между чипами, пилюлями и карточками (пилюли добавляли второй) — OTA `9ba72122`                                                       |
| 11:10 | **Карточка фактуры в две строки**: «Faktura №1 · фирма» / «дата · сумма», большие ⋮ справа — OTA `ed59c283`                                                     |
| 10:52 | Срок оплаты и фирма поменялись местами — OTA `1e430cea`                                                                                                         |
| 10:46 | **Бейдж убран**, сумма встала на его место, ⋮ остались одни справа — OTA `b82cfc6a`                                                                             |
| 09:50 | **⋮ в своей колонке** (48dp, вертикальные), бейдж и сумма левее — OTA `7f2b6607`                                                                                |
| 09:46 | **Задачи**: список открывается на невыполненном, фильтры — статусы (открытые / просроченные / готовые / все), внутри — по срочности — OTA `35130e08`            |
| 08:58 | **Одна шкала отступов на всё приложение**: `src/theme/spacing.js` + `docs/SPACING.md` (гаттер 20, паддинг карточки 20, между карточками 12) — OTA `bcfbae07`    |
| 08:53 | **Списки**: один зазор между чем угодно — карточка/карточка, заголовок/карточка, над и под заголовком секции — OTA `6031b972`                                   |
| 08:49 | **Приветственный тур** — только при первом входе пользователя (ключ на пользователя + 14 дней «уже освоился») — OTA `fb9c68bd`                                  |
| 08:42 | **Копия документа** + весь ряд статусов виден сразу — OTA `11666874`                                                                                            |

## 12:53 · Тёмная тема: «Шрифты» (скрин Aviseringar)

- [x] Описания пунктов уведомлений были марин-синими (`rgba(5, 45, 80, 0.55)`) на почти чёрном фоне — не читались. Это не один экран: тот же жёстко вбитый синий нашёлся в 18 файлах
- [x] Прошёл по всем `*.styles.js` и по JSX-пропсам: текст → `textMuted` / `placeholder`, линии → `divider`, мягкие заливки → `surfaceMuted`. `FieldIcon` без явного цвета берёт цвет текста темы, плашка фото инструмента — из темы
- [i] На светлых темах приглушённый текст при этом сдвинулся с «синий на 55%» на серый iOS-палитры — тот же, что уже стоит в токенах
- [i] Намеренно НЕ тронуто: светлые токены в `colors.js`, светлая ветка попапа, затемнения под модалками, welcome-слайды и стрелки карусели файлов (они лежат на фиксированной светлой подложке), селектор цветной темы
- 📦 OTA `57c4904f`

## 12:04–12:44 · Навигация и выбор проекта

- [x] «Выбрал проект — вернулся»: в режиме выбора нет ни поиска, ни лишних плашек, «Alla projekt» — строка-заголовок над списком
- [x] Нижний бар и кнопка «назад» на внутренних страницах = главная 1:1 (та же поверхность, рамка, свечение; стеклянные слои убраны)
- [x] Тёмная тема на экране адреса: панель карты, подсказки и бейджи перестали быть светлыми плашками
- 📦 OTA `888243ba` / `f27c0b00` / `c52b5439` / `d4d861d6`

## 09:46–11:47 · Задачи и карточка документа

- [x] Фильтры задач переосмыслены: статусные чипы (открытые / просроченные / выполненные / все) вместо списка проектов, сортировка по срочности, группы проектов сворачиваются
- [x] Карточка фактуры по нарисованному скрину: бейдж убран, сумма наверх, ⋮ большие и вертикальные в своей колонке справа, две строки, фирма 14, пилюли заметнее
- [x] Удаление и копия документа доступны из «⋮», не только свайпом
- 📦 OTA `35130e08` … `81d85e46`

## 08:42–08:58 · Одна система отступов

- [x] `/deep-research` по UX-расстояниям → `src/theme/spacing.js` (4·8·12·16·20·24·32) и роли (`screenGutter` 20, `cardPadding` 20, `betweenCards` 12, `insideCard` 8, `listBottom` 140), правило записано в `docs/SPACING.md`
- [x] `EntityListScreen` владеет всеми зазорами списка — экран своих чисел не пишет. Двойные зазоры (пилюли + список) убраны
- [x] Android проверен тем же кодом — шкала общая, платформенных исключений не заводили
- 📦 OTA `bcfbae07` / `6031b972` / `9ba72122`

## Открыто с этого дня

- [ ] **Цветная тема против Figma** — геометрию так и не сверил: Figma REST отдавала 429. Токен даётся на сессию, нужно попросить заново
- [ ] **Релиз 1.1.2** — билды iOS 188 / Android 27 готовы, `eas submit` не запускал, тексты «What's new» не написаны
- [ ] **Android-геолокация** — решить: оставляем foreground service с уведомлением или перепроверяем Play Services Geofencing под флагом

# 2026-09-20 — дневная сессия (скриншоты в чате)

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 22:46 | **Входящие счета**: новый экран Leverantörsfakturor, уведомление «неоплаченная фактура» открывает нужный счёт; «•••» по Material (вертикальные, 48dp, угол карточки) — OTA `66b10e89` |
| 22:39 | **Уведомление геолокации** больше не всплывает при каждом запуске (поток перезапускался зря) — OTA `b2344196` |
| 22:31 | **Действия над документом из списка**: отправить почтой / скачать-поделиться / отметить оплаченной; цветные пилюли статусов; «Alla kunder» убран — OTA `4f8d9f76` |
| 22:20 | **Строка фактуры**: моms приходит с артикулом (выбор ставки убран), артикул виден по названию, Dröjsmålsränta убрана — OTA `fe6b1db2` |
| 22:12 | **Артикулы не показывались** в выборе (импорт из бочки, где их нет) + одно правило отступов на главной — OTA `b5a6bebe` |
| 22:08 | **Чеклист наследуется фирмой** (клиент/артикул/документ), уведомление геолокации переведено — OTA `4fcc4680` |
| 22:05 | **Фактура**: нижние кнопки снова отвечают (застревал флаг анти-дабл-тапа), системная строка по теме, «•••» Register убран из списков — OTA `6a83c55e` |
| 21:58 | **Строки документа**: артикул перед описанием; тап в числовую ячейку выделяет значение — OTA `dda92575` / `88ca0a4a` |
| 21:52 | **Пикеры**: линии строк тоньше и в цвет темы, под последней строкой линии нет — OTA `a6117987` |
| 21:48 | **Выбор проекта в фактуре**: у всех, кроме суперадмина, список был пуст (403 на `GET /projects`) → `/projects/my`; галочка сохранения в шапке всех форм — OTA `8f07bfef` |
| 21:43 | **Ny klient**: сохранение из шапки, byggmoms только для фирм, фамилия не обязательна — OTA `e5f3699f` |
| 21:35 | **Онбординг**: второй трек снова доступен (карточка пряталась после первого), данные фирмы наследуются всеми — OTA `331a1a3d` |
| 21:28 | **Рапорт**: строки начинаются под крестиком и держат общую правую границу — OTA `0b12fc51` |
| 21:20 | **Онбординг**: шаг «Rapportera din tid» не мог отметиться НИКОГДА (сводку месяца считали списком) — OTA `34652aa3` |
| 21:12 | **Задачи**: колокольчик и чекбокс в тоне и размере текста задачи (чернила на 50%), одна пара во всех темах — OTA `f7c4452c` / `c685c0b1` |
| 21:03 | **Тестовые PDF** фактуры и оферты на рабочий стол — локальный рендер боевых шаблонов, в прод ничего не писалось |
| 20:56 | **Тексты сторов** в `docs/STORE_LISTING_COPY.md` + запись в память (GPS **и вручную**, проекты, задачи, планирование, инвойсинг) |
| 20:50 | **Версия 1.1.2** + билды: iOS 188, Android 27 (profile production, runtime 1.1.0) |
| 20:45 | **Дагсрапорт**: первая строка не наезжает на ×; **Android**: убрана серая полоса-тень у карточки онбординга — OTA `15985fb1` / `c0f20534` |
| 20:40 | **Цветная тема по Figma**: карточка Shifts `#282828` вместо размытого серого, бейджи 60%; текст бейджа в цвет подписи карточки — OTA `d4185058` / `596eda33` |
| 20:35 | **Фактура/оферта 1:1 с админкой**: артикулы в строках, скидка, ставка НДС, текстовые строки, срок оплаты + пеня, timpris/артикул труда, город в адресе — OTA `27c153c9` |
| 20:02 | **Аудит по темам**: нашёл одну утечку (типографика подписи карточек шла всем темам) — вынес в токены темы; синяя тема теперь тронута только стрелкой селектора — OTA `f5c650b1` |
| 19:55 | **Область правок сузил**: приглушённые чернила — только светлые темы, бар-карточка — только тёмная; сплошная рамка бара (рисуется поверх свечения) — OTA `24bcec52` / `33b630f1` |
| 19:49 | **Главная**: иконки карточек в цвет подписи, селектор проекта приглушён; **Anpassa** — пилюли ниже (40) и зазоры шире (14) — OTA `0dacbf09` / `d4b31d4b` |
| 19:35 | **Тёмная тема**: бар = карточка (заливка+рамка+свечение), белый Play с синим глифом по Figma, мягче чернила на светлых темах — OTA `a5bfdc21` / `7d72ba70` |
| 19:28 | **Тёмная тема по Figma** (обводка карточек #FFF 20%) + нижний бар = просто бордер + подписи карточек в цвет текста блоков — OTA `00563a6d` / `5530439e` |
| 19:20 | **Главная**: подписи карточек как заголовки блоков, белая стрелка селектора, нижний бар в стиле карточек, Anpassa на размер меньше — OTA `ab0f6a23` |
| 19:02 | **Kom igång**: два варианта вместо трёх, формулировки 1:1 с админкой (11 языков) — OTA `eb8765fc` |
| 18:33 | **Kom igång**: вернул шаг «Lägg till artiklar» в трек «работаю один» + шаги ведут в форму создания, а не в список — OTA `1e1f2d11` |
| 18:30 | **Меню**: открывается на Inställningar, остальные разделы свёрнуты — OTA `1e1f2d11` |
| 11:33 | **Онбординг**: «Anpassa startsidan» оставался отмеченным со старого флага — версионировал ключ хранения — OTA `f8f2852e` |
| 11:20 | **Skapa uppgift**: inset-разделители как в настройках, заметки к заголовку, порядок блоков — OTA `3b80b5f3` |
| 11:17 | **Verktyg**: удаление инструмента свайпом влево (как в списке проектов) — OTA `c84bec3c` |
| 11:01 | **Создание задачи как в админке**: Prioritet + Upprepa + дефолты дат — OTA `e997233a` |
| 10:01 | **Heldag** вырезан отовсюду — OTA `5042b445` |
| 08:58 | **Онбординг**: «Anpassa startsidan» отмечался от простого открытия дровера — OTA `80e73987` |
| 08:55 | **Инструменты**: пустые списки проектов/работников (403 на `/users/role/worker` + `Promise.all`) + сканирование QR при создании — бэкенд `1c85b4f`, моб. `f90bbb2e` — OTA `80e73987` |
| 08:52 | **Бэкенд**: ProjectAdmin не видел созданный им инструмент — `530701f` (прод) |
| 08:41 | **Home**: пустая карточка Anteckningar в общем стиле — иконка по центру — OTA `3cd2996a` |
| 08:38 | **Skapa projekt**: убрал кнопку Avtal + карточку AVTAL, Ekonomi → 3 блока по 2 безрамочные строки (`FloatingField`) — OTA `e246b544` |
| 08:15–08:22 | Перевёл рабочий файл в один `WORKLOG.md` (был `TZ_TASKS.md`), добавил секцию «Очередь» |

## 22:46 · Входящие счета + правильные «три точки»

- [x] **Уведомление «Obetald faktura» вело в никуда**: пуш нёс `type: supplier_invoice_due` и id счёта, но маршрут вёл на `Economy` — это ИСХОДЯЩИЕ фактуры, где входящих счетов нет вовсе
- [x] **Новый экран `SupplierInvoicesScreen`** (Leverantörsfakturor): фильтры Obetalda / Betalda / Alla, свайп вправо или тап — «оплачено», бейдж просрочки, сумма, поставщик. Счёт из уведомления поднимается в начало списка
- [x] Маршрут пуша ведёт на него; пункт добавлен в меню (Ekonomi); «Mitt arbete» на главной больше не ведёт на исходящие
- [x] **Ресёрч по «•••»** (Material): тач-таргет минимум **48×48 dp** (было 20px + паддинг 2), overflow-меню ставится **в правый верхний угол карточки**, используется **вертикальный ⋮**. Сделал ровно так, бейдж и сумму сдвинул левее
- 📦 OTA `66b10e89`

## 22:39 · Уведомление геолокации при каждом запуске — наш баг

- [x] Флаг «foreground service поднят» жил в области модуля и обнулялся с каждым новым процессом (и с каждым OTA) → при открытии приложения поток геолокации останавливался и запускался заново → Android заново публиковал уведомление
- [x] Теперь фоновая задача оставляет отметку времени на каждом тике, а синхронизация перезапускает поток только если отметке больше 6 минут (то есть поток реально замёрз — тот случай, ради которого перезапуск писался). Два новых теста
- 📦 OTA `b2344196`

## 22:31 · Что можно сделать с документом из списка

- [x] Тап или «•••» на карточке → лист: **отправить почтой**, **скачать / поделиться** (PDF в системную шарилку), **отметить оплаченной** (для фактур)
- [x] При создании фактуры без e-mail теперь предлагает «Ladda ner i stället» вместо тупика
- [x] Пилюли статусов в своих цветах (та же палитра, что у бейджа), «Alla kunder» убран — фильтр по типу клиента стандартными чипами
- 📦 OTA `4f8d9f76`

## 22:20 · Строка фактуры: моms, артикул, пеня

- [x] **Выбор ставки НДС из строки убран** — ставка приходит с артикулом, она зафиксирована в каталоге
- [x] **Выбранный артикул виден**: название крупно, под ним номер и ставка (было «Artikel 1»)
- [x] **Dröjsmålsränta убрана** из формы целиком
- 📦 OTA `fe6b1db2`

## 22:12 · Артикулы не показывались + одно правило отступов

- [x] **Баг**: `ArticlePickerModal` импортировал `articleService` из `services/index.js`, который его не экспортирует → `undefined` → вызов падал → «Inga artiklar» при полном каталоге. Импорт напрямую + добавил экспорт в бочку
- [x] **Одно правило отступов** (по отмашке): желоб экрана 20 → края карточек, внутренний отступ карточки 20, **весь текст на линии 40**, включая заголовки секций (как в сгруппированных списках iOS). Заголовки 4 → 20, цветная тема 14 → 20, карточка онбординга 16 → 20
- 📦 OTA `b5a6bebe`

## 22:08 · Чеклист наследуется фирмой

- [x] Клиент, артикулы и первый документ отмечаются, как только они есть **у фирмы** (по отмашке «да, конечно»). Задачи и инструменты остались персональными
- [x] Уведомление геолокации переведено на 11 языков (было захардкожено по-английски)
- 📦 OTA `4fcc4680`

## 22:05 · Фактура: мёртвые кнопки, системная строка, лишнее меню

- [x] **Нижние кнопки фактуры не отвечали** — настоящий баг: `submittingRef` (защита от двойного тапа) взводился, а на выходе «нет e-mail» не сбрасывался → после одной попытки «Skapa & skicka» без почты обе кнопки были мертвы до ухода с экрана
- [x] **Системная строка** (часы/сеть/батарея) была прибита к `style="dark"` — на тёмной теме чёрное на чёрном. Теперь берёт цвет из темы (`ThemedStatusBar` в App.js)
- [x] **«•••» Register убран** из списков фактур и оферт: клиенты, артикулы и данные фирмы — отдельные сущности со своими экранами, а этот экран делает документ. Осталось одно действие
- [x] **Ресёрч по уведомлению** (22:20, Alexander: «сделай ресёрч, думаю ты по ушам ездишь»). Проверил по докам Android и по нашему же коду:
  - уведомление у foreground service **обязательное и постоянное**, не периодическое («Foreground services show a status bar notification» — developer.android.com/develop/background-work/services/fgs); «раз в день» невозможно
  - **Android 13+**: пользователь может смахнуть уведомление FGS сам, а если отключить разрешение на уведомления — оно вообще не попадёт в шторку (останется только в Task Manager). Это только про него, остальные каналы не трогаются
  - канал у expo-location создаётся с `IMPORTANCE_LOW` (`LocationTaskService.kt:103` в node_modules) → без звука и без всплывающего баннера
  - **альтернатива без уведомления существует**: Play Services Geofencing (PendingIntent, работает при закрытом приложении, задержка 2–3 мин, до 6 мин если телефон лежит). НО мы её уже пробовали и отказались: коммит `ca0f2832` (10.08.2026) — «Geofencer: registration not permitted» на строгих Android-сборках, enter/exit не срабатывали вообще
- [ ] **Решить по уведомлению**: (а) оставить как есть + перевод (сделан), (б) перепроверить Play-геофенсинг на текущем телефоне под флагом — если регистрация пройдёт, FGS и уведомление на Android можно убрать совсем, ценой задержки 2–6 мин
- [x] ~~**Уведомление «Shift location active»**~~ (поднято 21:39). Alexander: «зачем оно, если человек и так на смене — хватит раз в день». Разобрался: это обязательное уведомление Android-foreground-service, без него система убьёт фоновую геолокацию, и «раз в день» технически невозможно. Что реально можно и осталось сделать: (1) сделать его тихим/низкого приоритета, чтобы не всплывало баннером, (2) перевести текст — сейчас английский в шведском интерфейсе, (3) не перевзводить сервис без нужды, чтобы баннер не показывался заново
- 📦 OTA `6a83c55e`

## 21:58 · Строки документа

- [x] **Артикул перед описанием** — как на самой фактуре: сначала Art.nr, потом что сделано. Выбор артикула подставляет номер, название, цену, единицу и НДС
- [x] **Тап в числовую ячейку выделяет значение** (`selectTextOnFocus`) — во всех полях фактуры и оферты: количество, единица, цена, скидка, срок оплаты, сумма ROT. Раньше надо было вручную стирать «0»
- 📦 OTA `dda92575`, `88ca0a4a`

## 21:52 · Линии в пикерах

- [x] Сплошная светлая линия во всю ширину выглядела наклеенной поверх тёмного листа. Теперь волосяная линия цветом разделителя темы, под последней строкой линии нет
- 📦 OTA `a6117987`

## 21:48 · Выбор проекта был пуст у всех, кроме суперадмина

- [x] Пикер звал `GET /projects`, а он в бэкенде **только для суперадмина** → 403 → «Inga projekt» даже когда у фирмы полно проектов. Теперь `/projects/my`, как в вебе. Выбор проекта по-прежнему кладёт littera в Orderreferens, а название — в первую строку
- [x] **Галочка сохранения в шапке** у всех форм создания (артикул, оферта, фактура, клиент) — чтобы не листать до конца
- 📦 OTA `8f07bfef`

## 21:43 · Ny klient 1:1 с админкой

- [x] Сверил поля с `ClientCreateForm`: наборы совпадают (фирма: название/кундnr/орг.нр/момсрег/контакт; частник: имя/фамилия/личный номер/кундnr; далее адрес, контакты, оплата, заметки)
- [x] Отличия, которые исправил: **byggmoms только у фирм** (у частника тумблера нет и в payload не уходит), **фамилия не обязательна** (в вебе обязательно только имя), **сохранение из шапки**
- 📦 OTA `e5f3699f`

## 21:35 · Онбординг: второй трек и наследование данных фирмы

- [x] **Второй трек нельзя было начать**: когда трек закрывался, карточка «Kom igång» пряталась целиком (`allDone` считался по текущему треку) — вместе с ней исчезала дверь во второй. Теперь карточка уходит только когда закрыты оба, а завершённый трек отдаёт эстафету строкой «Bra jobbat! Nu till pengarna.»
- [x] **Данные фирмы наследуются**: шаг «Fyll i företagsuppgifter» был персональным (`detailsUpdatedByUserId === я`) — второму админу предлагали заполнить то, что уже печатается на его фактурах. Теперь шаг закрывается фактом наличия данных у фирмы. Инструменты/клиенты/артикулы пока остались персональными — ждёт решения
- [i] Проверил доступ: `/clients`, `/articles`, `/offers`, `/invoices` уже отдают **всё по companyId**, без фильтра по создателю. Пустой список = другая фирма, а не права
- [ ] **Решить и сделать**: наследовать ли так же галочки «Lägg till en klient», «Lägg till artiklar», «Skapa offert eller faktura» (сейчас персональные — `createdByUserId === я`). Alexander сказал «родитель заполнил → дети наследуют», но раньше требовал обратного для инструментов — нужна одна формулировка
- 📦 OTA `331a1a3d`

## 21:25 · «Как сделать одно правило по отступу текста»

- [i] Замерил по скриншоту: заголовки секций на 20 pt, край карточки 20, содержимое карточки 40, текст в пилюле 47 — текст живёт на трёх разных линиях
- [ ] **Правило предложено, НЕ применено** (ждёт отмашки, потому что сдвинет каждый заголовок на экране): желоб экрана 20 → края карточек; внутренний отступ карточки 20; **весь текст, включая заголовки секций, на линии 40** — заголовок равняется по тексту внутри карточки, как в сгруппированных списках iOS. Плюс привести к одному числу разнобой отступов карточек: сейчас 14 (цветная тема), 16 (карточка онбординга), 20 (остальные)

## 21:28 · Рапорт: строки под крестиком

- [x] Первая строка рапорта начинается под ×, а не протискивается мимо него → правая граница значения совпала с правой границей чекбокса в карточке задач. Одна вертикаль по всему экрану
- 📦 OTA `0b12fc51`

## 21:20 · Онбординг: шаг «Rapportera din tid» был мёртвый

- [x] Alexander добавил 8 ч вручную за 20 сентября — галочка не встала. Причина: проверка считала ответ `/shifts/history` **списком** (`length > 0`), а эндпоинт возвращает **сводку месяца** (итоги + массив дней внутри). У объекта длины нет → шаг не мог отметиться НИКОГДА, ни от GPS, ни от ручных, ни от плановых часов
- [x] Теперь читаю часы из сводки (`monthTotalDurationMs` / `monthManualDurationMs` / `monthPlannedDurationMs` / `previousMonthTotalDurationMs` + дни) и засчитываю любой источник
- 📦 OTA `34652aa3`

## 21:12 · Колокольчик и чекбокс в карточке задач

- [x] Были: синий колокол + белый залитый квадрат разных размеров. Стали: чернила карточки на **50%** — ровно как текст задачи (`projectText`), колокол 16, квадрат 17 с обводкой 1.5, пустой. Пара берётся из темы → одинаково во всех темах
- 📦 OTA `f7c4452c` (до этого `c685c0b1`, `15985fb1`)

## 21:03 · Тестовые PDF на рабочий стол

- [x] `~/Desktop/ByggExp-testfaktura.pdf` и `~/Desktop/ByggExp-testofferten.pdf`. Отрендерил **локально** из боевых шаблонов бэкенда (`invoice-pdf.template.ts` / `offer-pdf.template.ts` + тот же puppeteer), клиент выдуманный — **в прод не писалось ничего**
- [i] Скрипт рендера — в scratchpad сессии; если понадобится снова, проще написать заново (5 строк: импорт шаблона → `page.setContent` → `page.pdf`)
- [i] На фактуре видно всё, что чинили: Betalningsvillkor, Förfallodatum, Dröjsmålsränta, orderreferens, скидка по строке, текстовая строка, ROT с персональным номером, адрес с городом

## 20:56 · Тексты для сторов

- [x] `docs/STORE_LISTING_COPY.md`: требование — описание должно говорить, что время считается **автоматически по GPS И вводится вручную**, плюс проекты, задачи, планирование, инвойсинг. Черновики sv+en под лимиты (Play 80 знаков, App Store подзаголовок 30) + абзац промо
- [x] Продублировано в память (`project_store_listing_copy.md`), чтобы не потерялось между сессиями

## 20:50 · Релиз 1.1.2

- [x] `app.json` version → **1.1.2** (1.1.1 занята в обоих сторах), `runtimeVersion` оставлен **1.1.0** — нативного не менялось, новые бинарники получают те же OTA
- [x] Билды запущены: **iOS 188**, **Android 27**, профиль production, канал production — обе сборки FINISHED
- [ ] **Отправить в сторы**: `eas submit -p ios --latest` и `eas submit -p android --latest`
- [ ] **«Что нового» для App Store** — текст не написан
- [ ] **Обновить описания в Play Console и App Store Connect** по `docs/STORE_LISTING_COPY.md`

## 20:45 · Дагсрапорт + тень на Android

- [x] Первая строка рапорта стартует ниже и держит отступ справа — часы больше не заезжают под ×
- [x] **Android**: у карточки онбординга была серая полоса с жёстким краем — Android рисует elevation-тень по контуру и кладёт её ПОД заливку, а заливка полупрозрачная → тень просвечивала. На Android карточка непрозрачная + волосяная рамка, iOS сохранил мягкую тень
- 📦 OTA `15985fb1`, `c0f20534`

## 20:40 · Цветная тема по Figma + цвет текста бейджей

- [x] Карточка Shifts — сплошной `#282828` (было `rgba(40,40,40,0.7)` → на экране `#636363`), бейджи 60% как в Figma (сила заливки вынесена в токен темы `infoBadgeAlpha`, у остальных тем остались спокойные 20%)
- [x] Текст бейджа больше не белый на бледной заливке — берёт цвет подписи карточки
- [ ] **НЕ досверено**: геометрия цветной темы (высота карточки, радиус, зазоры сетки, кегль подписи) — Figma REST отдавал `429 Rate limit exceeded`. Продолжить с: файл `vXrFV6ymlOokXGiZCQP6NE`, страница «Главные» `187:2883`, фрейм «iPhone 14 & 15 Pro – 57» / `Frame 5870` (сетка 341×427). Токен Figma одноразовый — просить заново
- 📦 OTA `d4185058`, `596eda33`

## 20:35 · Фактура и оферта — флоу как в админке

Сам PDF никогда не отличался: его рисует бэкенд по одному шаблону (`invoice-pdf.template.ts` / `offer-pdf.template.ts`) — что из админки, что из приложения. Отличалось то, ЧТО приложение могло в него положить.

- [x] **Строки**: кнопка выбора артикула из каталога (подставляет номер, название, цену, единицу и ставку НДС, не затирая уже введённые описание/цену — то же правило, что в вебе), поле скидки, ставка НДС по строке (25/12/6/0), и текстовая строка-заголовок, которая не идёт в итоги (`isText`, как `TEXT_ITEM` в вебе)
- [x] **Срок оплаты** — редактируемое поле в днях, двигает дату оплаты; **дröjsmålsränta** отправляется. Раньше обе строки печатались пустыми
- [x] **Timpris и артикул труда клиента** падают в строки-часы: при выборе клиента и когда в строке набирают единицу «tim»
- [x] **Город вернулся в адрес получателя**: шаблон печатает `postalCode` как целую строку и `city` не читает вовсе — веб кладёт туда «индекс город страна», приложение слало голый индекс
- [x] **`representative`** (контактное лицо клиента) — резервное значение шаблона для «Er referens»
- [x] Новый `ArticlePickerModal`, ключи i18n на 11 языках, 4 новых теста (текстовые строки и часовые единицы) — 351 зелёный
- 📦 OTA `27c153c9`

## 20:02 · Аудит: что куда попало по темам

Поднял все правки главного экрана за сессию и сверил область действия каждой.

**Утечка была одна:** типографика подписи карточек (15px / regular / вес 400) — просил ты её на бело-голубой теме, а я записал в общий токен, и её получили синяя, тёмная и colorful. Вынес в токены темы (`labelSize`, `labelFamily`, `textWeight`): облегчённая подпись теперь только у бело-голубой и серой, у остальных вернулись 16px / 600.

**Остальное оказалось уже по темам:**

| Правка                                                    | Тема                                                                              |
| --------------------------------------------------------- | --------------------------------------------------------------------------------- |
| Чернила `#24303B` / `#464646` (заголовки, иконки, шеврон) | только светлые                                                                    |
| Подпись и иконки карточек на 70%                          | только темы с тёмной подписью (светлые)                                           |
| Приглушённый текст селектора                              | только светлые                                                                    |
| Обводка карточек `#FFF` 20% по Figma                      | только тёмная                                                                     |
| Нижний бар как карточка + свечение + сплошная рамка       | только тёмная                                                                     |
| Белый круг Play с синим глифом                            | только тёмная                                                                     |
| Белая стрелка вниз в селекторе                            | базовая, светлые темы красят своей — **единственная правка, затрагивающая синюю** |

Синяя, зелёная, оранжевая и colorful возвращены к прежнему виду: матовое стекло у бара, тёмно-синие иконки бара, белые подписи в полную силу, 16px/600.

## 19:55 · Рамка бара сплошная + правки только там, где просил

- [x] **Рамка нижнего бара не рвалась** — причина: это был настоящий `border` у пилюли, а border принадлежит родителю, и любой ребёнок рисуется поверх него. Угловое свечение — ребёнок, оно и замывало нижне-правую дугу. Теперь рамка рисуется отдельным слоем ПОСЛЕ свечения и замыкается по всему кругу
- [x] **Синяя тема — шрифты и иконки как было**: белые подписи на градиенте остаются в полную силу (приглушение на 70% делало их плохо читаемыми). Приглушённые чернила теперь только у тем с тёмным цветом подписи — бело-голубая и серая
- [x] **Нижний бар-карточка — только тёмная тема**. Синяя, зелёная, оранжевая и светлые темы вернулись к матовому стеклу и своим прежним цветам иконок
- 📦 OTA `24bcec52` + `33b630f1`

## 19:49 · Иконки в цвет шрифта, серый селектор, воздух в Anpassa

- [x] **Иконки карточек** — того же цвета, что подпись (тот же приглушённый чернильный на 70%): карточка читается как один объект, а не «тёмная иконка + светлая подпись». Цветные карточки темы colorful сохраняют белые иконки
- [x] **Выбор проекта** — приглушён так же: пока проект не выбран, это плейсхолдер, а не заголовок, и не должен спорить с часами под ним
- [x] **Anpassa** — пилюли 44→40, зазоры 10→14, отступы под заголовками секций шире. Был сплошной штабель плашек, теперь список читается строками
- 📦 OTA `0dacbf09` + `d4b31d4b`

## 19:35 · Чернила мягче, бар = карточка, Play по Figma

- [x] **Чернила светлых тем на шаг от чёрного**: `#010D18` → `#24303B` (pale-blue) и `#282828` → `#464646` (серая). Одно значение красит заголовки, иконки карточек, иконки бара и шеврон селектора; подписи карточек едут на нём же на 70%, поэтому светлеют вместе
- [x] **Нижний бар = карточка**: та же заливка, та же волосяная рамка, то же угловое свечение (`cardGlow`, тот же SVG-градиент, что у карточек). Заливка перед этим «сплющивается» на цвет страницы (новый хелпер `flattenColor`) — поверхность карточек полупрозрачная, и сквозь неё просвечивали края карточек под баром, отчего линия выглядела рваной
- [x] **Play в тёмной теме — по Figma** (`actionBtn`, 124×124): белый круг + глиф `#3097F7`. Инверсия светлых тем, где цвет на круге, а глиф белый
- 📦 OTA `a5bfdc21` + `7d72ba70`

## 19:28 · Figma-сверка тёмной темы + бар + цвет подписей

**Figma подключилась по REST API с твоим токеном — платная версия не нужна** (файл `vXrFV6ymlOokXGiZCQP6NE`, страница «Мобильное приложение», тёмный Home = нода `157:10684`).

- [x] **Карточки в тёмной теме** — по инспектору Figma (Frame 5767, 155×115): заливка `#232323`, обводка `#FFFFFF` 20% внутрь, толщина 1, радиус 20. У нас обводка была `transparent` с комментарием «в Figma штриха нет» — неверно, вернул
- [x] **Нижний бар** — заливка теперь непрозрачная, цветом самого экрана (нижняя точка градиента) + та же волосяная рамка. Полоски «прерывались» потому, что заливка была полупрозрачной поверхностью карточек и края карточек просвечивали сквозь бар
- [x] **Подписи карточек** — цвет текста из блоков снизу («Inga arbetspass hittades ännu.»): это цвет лейбла на 70% (`#010D18B3` поверх белого = замеренный `rgb(76,85,93)`). Берётся из темы, поэтому в тёмной теме это белый на 70%, а не захардкоженный серый
- 📦 OTA `00563a6d` + `5530439e`

## 19:20 · Четыре правки по скриншотам (шрифт, стрелка, бар, Anpassa)

- [x] **Подписи карточек** (Anställda / Arbetspass / Projekt / Uppgifter) — как заголовки блоков снизу: 15px, обычное начертание. Было 16px/600. Цвет не трогал — он и так одинаковый, замерил пиксели: и там, и там `rgb(3,12,23)`, разница была только в жирности (плотность краски 0.20 против 0.13)
- [x] **Стрелка в селекторе проекта** — белая по умолчанию, как текст и обводка рядом; светлые темы (lightBlue/colorful) по-прежнему красят её своим цветом через `iconStyle`
- [x] **Нижний бар** — теперь носит поверхность карточек: их заливка, их волосяная рамка, их цвет иконок. Ни блюра, ни стеклянных градиентов, ни тени. Передаётся только с главной — на остальных экранах бар остался прежним (матовое стекло)
- [x] **Ящик Anpassa** — на размер меньше: пилюли 52→44, подписи 17→15, кружки тем 44→36, зазоры 14→10. Шаг drag-списка пересчитан (`ITEM_HEIGHT`/`GAP`), перетаскивание не поехало
- 📦 OTA `ab0f6a23`

## 19:02 · «Пока оставь два варианта и сделай как в админке»

- [x] Вопрос первого запуска теперь с двумя ответами, как в вебе: **«Hantera projekt eller arbetslag»** и **«Skicka faktura eller offert»** — формулировки взяты слово в слово из `byggexp-admin` и переведены на 11 языков (раньше приложение и админка спрашивали об одном и том же разными словами)
- [x] «Jag jobbar själv» больше не предлагается, но трек и его пресет главной остались: у того, кто уже ответил так (в том числе у тебя), раскладка и чеклист не поменяются
- 📦 OTA `eb8765fc`

## 18:33 · «Посмотри логику онбординга в админке — тут проёб с артикулом»

- [x] **Сверил с веб-чеклистом** (`byggexp-admin/src/features/onboarding/activation.js`). Там денежный трек — четыре шага: реквизиты → клиент → **каталог артикулов** → оффер/фактура. В приложении в треке «работаю один» артикулов не было, а описание последнего шага это прикрывало: «kund och artiklar läggs till på vägen». То есть соло-фирма доходила до счёта с пустым прайсом
- [x] Шаг `article` вернулся в трек «работаю один» (и стоит перед счётом, как в админке). Описание шага «Skapa offert eller faktura» переписано на 11 языках — теперь оно про то, что делает шаг, а не оправдывает пропущенный
- [x] Заодно шаги ведут туда, где дело заканчивается, а не в список: `CreateClient`, `CreateArticle`, `CreateTool`, `CreateInvoice` — ровно те же диплинки, что в вебе (`?create=1`). Раньше половина шагов высаживала на пустой список, где + надо искать самому
- 📦 OTA `1e1f2d11`

## 18:30 · «Чтобы при первом входе только Inställningar был открыт»

- [x] Меню открывается с развёрнутым «Inställningar», остальные четыре раздела свёрнуты (раньше по умолчанию был раскрыт «Projekt & arbete»). Один открытый раздел вместо простыни, остальные — в одно касание
- 📦 OTA `1e1f2d11`

## 18:16 · «Этот блок надо наверх» (скрин Meny)

- [x] Уточнил, какой именно (три прочтения), выбран «весь Inställningar над Projekt & arbete». Настройки теперь первый раздел меню: аккаунт, раскладка главной, язык и уведомления — то, ради чего сюда заходят; к самой работе идут с главной, а не через меню. Остальные разделы в прежнем порядке и с тем же состоянием свёрнутости
- 📦 OTA `106d8445`

## 18:10 · «Не понял, где смотреть» + «выбрал работать самому, а Anställda остался»

- [x] **Найден корень сегодняшних «не работает, нету»**: в `app.json` стоит `fallbackToCacheTimeout: 0` — OTA скачивается в фоне и применяется ТОЛЬКО при следующем запуске. То есть каждая правка выглядела как «не приехала», пока приложение не перезапустят ДВАЖДЫ. Теперь App.js при старте проверяет обновление, скачивает и перезагружается в новый бандл сразу. В dev пропускается, при ошибке сети просто работает текущий
- [x] Пресет теперь применяется и поверх существующей раскладки: явный ответ на вопрос — это заявление о том, как человек работает, а не безмолвный дефолт (раньше он не трогал сохранённую раскладку, а она есть у всех, кроме свежей установки → нажатие не давало ничего)
- [i] По скриншоту диагноз: на телефоне был старый бандл. В наборе «работаю один» нет Uppgifter, а он на экране — значит пресет не применялся, а Offert/Faktura Alexander добавил руками раньше
- [i] Где смотреть, какой бандл запущен: **Mitt konto** → внизу строка `Update: <id>`
- 📦 `OTA on launch` · OTA `26f22326`

## 18:00 · «Как не накосячить с choice-overload на главной — может всё убрать, он сам добавит?»

**Разобрал закон и наше состояние.** Админ на первом запуске получал 6 кнопок + 4 блока + 2 круглые + чеклист = 12+ равнозначных решений. Перебор подтверждён. Но пустую главную я делать не стал и объяснил почему: пустой экран не показывает, что умеет приложение (не знаешь, чего лишён — не пойдёшь добавлять); настройка требуется ДО первой пользы; и это не убирает выбор, а переносит те же 12 галочек в настройки, где контекста ещё меньше.

**Сделано (вариант В + А):**

- [x] Первый запуск — ОДИН вопрос, три ответа: «Работаю один» / «У меня бригада» / «В основном офферы и счета»
- [x] Ответ сразу раскладывает главную под себя (`HOME_PRESETS`): solo → смены, проекты, оффер, фактура + «Mitt arbete», заметки; team → смены, сотрудники, проекты, задачи + дагсрапорт, задачи; billing → оффер, фактура, проекты, задачи
- [x] До ответа дефолт урезан с 6 кнопок до 3 (смены, проекты, задачи) и с 4 блоков до 2
- [x] Для «работаю один» — свой список Kom igång: без «пригласи команду», зато реквизиты, клиент, счёт
- [x] Пресет НИКОГДА не перезаписывает раскладку, которую человек настроил сам
- [x] Из меню не убрано ничего: главная — рабочий стол, а не каталог. Скрыто ≠ потеряно
- 📦 `856e1395` · OTA `e42c6f90`

## 17:40 · «Ручные часы оранжевые, GPS зелёным? обсудим, как правильнее»

**Моя позиция (принята):** цветом показываем ЧТО МОЖНО СДЕЛАТЬ и ЧТО НЕ ТАК, а источник часов — словом.

- [i] Почему не «оранжевый = ручные, зелёный = GPS»: (1) оранжевый уже нёс два смысла сразу — «ручные» и «сюда можно нажать»; (2) зелёный/оранжевый читается как оценка: GPS честный, ручные подозрительные — а ручные часы сплошь и рядом правда (сел телефон, подвал, забыл старт); (3) оранжевый с зелёным — худшая пара для дальтоников, а это каждый двенадцатый мужчина
- [x] Числа нейтральные, оба. Главное число само говорит источник словами: «5 h · manuellt», «16 h · GPS»
- [x] Синий — строки-действия: добавить часы, добавить рапорт, подтвердить
- [x] Зелёный оставлен под одно значение — «Attesterad»
- 📦 `5e8e67a4` · OTA `f4eb7a12`

## 17:18 · «Каша-мала: одно и то же время на двух разных проектах»

**Разбор по данным.** В карточке показывается ОГИБАЮЩАЯ смены (первый старт → последнее завершение), а `durationMs` — сумма отрезков (segments). Поэтому смена с паузами выглядит сплошным куском. Две смены пересекались легально: бэкенд запрещал только вторую АКТИВНУЮ, а «пауза + старт на другом объекте» был штатным сценарием переезда. Настоящая беда: у второй смены GPS 0 h (стартовала и не накопила ни одного отрезка), а сверху объявлено 9 ручных часов → за день 16 h + 9 h = **25 часов**, и никто не остановил.

**Решение Alexander'а: смены вообще не могут пересекаться по времени.**

- [x] Старт смены: открытая смена — активная ИЛИ на паузе — должна быть завершена. Раньше пауза не мешала стартовать вторую
- [x] Ручные часы на дату: окно проверяется на наложение с уже существующими сменами этого работника в этот день, на ЛЮБОМ проекте → отказ вместо второй претензии на те же часы
- 📦 `d1bdfd9`
- [x] **Поправил сразу же**: Alexander напомнил, что при переключении проекта предыдущая смена закрывается. А в коде геозона ставит старую на ПАУЗУ и стартует новую — мой жёсткий запрет сломал бы автопереключение. Теперь старт на ДРУГОМ проекте сам закрывает открытую смену (причина `switched_project`), а не отказывает. Тот же проект не трогается: он возобновляется, а не открывается второй сменой — `e07560e`

- [x] **Настоящая причина**: `ShiftsScreen` НЕ перечитывал данные при возврате на экран — ни `useFocusEffect`, ни слушателя. Рапорт сохранялся на сервере, но карточка оставалась той, что была загружена при входе, и вечно показывала «＋ Lägg till». Добавил перечитывание на фокусе (первый фокус пропускается — хук и так грузит при монтировании) — OTA `13839f4b`
- [x] **Побочно найдено и починено: гейт деплоя бэкенда был красный сам по себе.** Все 4 e2e падали НА ИМПОРТЕ: (1) `sanitize-html` — ESM, и e2e-конфиг jest его не трансформировал (в юнит-конфиге правило было, в e2e — нет); (2) 14 схем объявляли enum-поле без `type:`, и Mongoose угадывал тип по метаданным декоратора — в собранном приложении угадывает верно, под ts-jest нет. Проставил `type: String` рядом с `enum:` — это корректно в любом случае — `a4b085c`
- [i] Проверил прод напрямую (`curl` по новым маршрутам → 401, а не 404): бэкенд деплоился и раньше, моя первая гипотеза «ничего не выезжало» была неверной. Хорошо, что проверил, а не заявил
- 📦 `0127e51e` · OTA `13839f4b`

## 16:04 · «Кто GPS-часы заполнять будет, почему они не подтягиваются?»

- [i] Они и подтягиваются — сами, из смены. На скрине это видно: карточка 13:58–05:59 показывает 16 h, и ровно эти 16 стоят у 17-го числа во вкладке GPS. Заполнять руками их нельзя и не нужно
- [x] Но вторая карточка (14:00–23:00) — день, заведённый ВРУЧНУЮ: такой день создаётся вообще без смены, `durationMs = 0`. Показывать у него «GPS-stämplad tid 0 h» неправильно: читается как «GPS не сработал», хотя смены просто не было. Теперь строка появляется, только если день реально отбивался и числа расходятся — для этого случая она и нужна
- 📦 `a42fa800` · OTA `628415b1`

## 15:50 · «Stämplat что значит? GPS часы?» → «назови грамматически корректно»

- [i] Объяснил три числа на карточке: сверху — часы, за которые день СЧИТАЕТСЯ (объявленные, иначе отметочные); «Manuella timmar» — что человек объявил сам; «GPS-stämplad tid» — что засекла смена (появляется, только когда числа расходятся)
- [x] Раз пришлось спрашивать — подпись не работала. «Stämplat» идёт от stämpelklocka (турникет), а у нас часы из GPS. Переименовал в **«GPS-stämplad tid»**: прилагательное + существительное, как у соседней строки «Manuella timmar»; голое причастие «GPS-stämplat» для подписи звучит обрубленно
- [x] Остальные 10 языков — смысловой эквивалент, а не калька («Время по GPS», «GPS-aeg», «Czas z GPS»)
- 📦 `e08e2ea4` · OTA `41971de8`

## 15:45 · «Верни синий, но 50% прозрачности… а лучше разными цветами, полупрозрачными» + «текст по левой стороне съезжает у пилюль»

- [x] **Разноцветные пилюли**: у каждого варианта свой оттенок — синий / янтарный / фиолетовый у типов часа, серо-синий / бирюзовый / зелёный у суточных. Выбранный заливается тем же цветом на 18%, подпись — полным цветом. Выбор виден, а единственным плотным блоком на экране остаётся «Spara» (Фон Ресторф: выделяться должно ровно одно)
- [x] **Левый край выровнен**: заголовки секций, подписи полей, ряды пилюль и строка ÄTA — всё теперь начинается с одних и тех же 16pt. Раньше пилюли и заголовки стояли на 4-12pt и ряд «уезжал»
- 📦 `149a7801` · OTA `d4ce5547`

## 15:36 · «На что она влияет, где отображается потом?» + «не сохраняется, как будто и не кликал» + «переключатели другим цветом»

**Справедливое замечание: я сделал ввод без выхода.**

- [x] **БАГ (главное)**: рапорт СОХРАНЯЛСЯ, но не возвращался. Список смен отдаёт явный набор полей (`SerializedShiftRecord`), и новых там не было — поэтому экран открывался пустым, а карточка вечно предлагала «＋ Lägg till». Теперь `hourType`, `travelKm`, `travelMinutes`, `perDiem`, `dayNote`, `ataId`, `reportedAt`, `approvedAt` едут вместе со сменой — `cf2c7c1`
- [x] **Куда это попадает (то, чего не было вовсе)**: Excel-выгрузка получила колонки Hour type / Travel km / Travel h / Per diem / Approved / Note, а колонка Duration теперь показывает часы, за которые день считается, а не только отметочные. Это и есть ответ «где отображается» — выгрузка уходит в зарплату
- [x] **Карточка смены** показывает содержимое рапорта строкой: «Övertid · 12 km · Helt» вместо безликого «Rapporterad»
- [x] **Пилюли перекрашены** с синего на чернильный `#052D50`: выбранный вариант — это состояние, а синий на экране принадлежит единственному действию «Spara»
- 📦 `b8b2c047` · OTA `b4092e51`

## 15:15 · «Не понимаю какая тут логика… если он 8:00 добавил, то они есть» + «окно вылазит ужасное» + «уменьши ещё адрес»

- [x] **Противоречие чисел.** Карточка писала «0 h» сверху и «8 h» в Manuella timmar. Оба числа правдивые: часы по отметкам = 0 (GPS не отработал), объявленные вручную = 8. Но читается как баг. Теперь в шапке — часы, за которые день СЧИТАЕТСЯ: объявленные, если объявили, иначе по отметкам. Если они расходятся, отметочные показываются отдельной строкой «Stämplat» — ничего не прячется
- [x] Объявленный ноль = «ничего не объявляли»: строка показывает «＋ Lägg till», а не оранжевый «0 h»
- [x] **Окно ручных часов** было центрированной карточкой, и цифровая клавиатура накрывала её собственные кнопки («Avbryt» обрезался). Теперь нижняя шторка, поднимается вместе с клавиатурой, действия разнесены: «Spara» — крупная, «Avbryt» и «Rensa» в подвале
- [x] Пара полей «h + min» заменена ОДНИМ десятичным полем: «7,5». «7,5» и «7.5» одинаково принимаются
- [x] **Адрес** ещё короче: оставляю улицу и город, выкидываю индекс и страну — карточке надо сказать, КАКОЙ объект, а не продиктовать почтовый адрес
- [x] 7 новых тестов (эффективные часы + короткий адрес), всего 315
- 📦 `0457087c` · OTA `3a1f59cc`

## 15:03 · «Ограничь адрес, неочевидно что можно нажать на 0m, и М убери — минуты тут никто считать не будет» (скрин Arbetspass)

- [x] **Минуты убраны везде**: длительность читается десятичными часами — «7,5 h», то самое число, что попадает в зарплату, вместо «7h 30m». Правило одно и в одном хелпере, поэтому карточка, итог месяца и ячейка календаря согласованы (в узкой ячейке единица опущена — колонка и так про часы)
- [x] **Адрес в одну строку** с обрезкой: раньше переносился на две и распирал карточку
- [x] **«＋» у кликабельных строк**: «Manuella timmar» и «Dagens rapport» показывали просто оранжевое «Lägg till», что читалось как подпись. Теперь у них плюс — доступное действие видно, а не запоминается
- 📦 `56246c88` · OTA `2a789193`

## 15:10 · «А если человек фотографировал чеки — тогда сумма расходов за сегодня»

**Твоя задача:** материалы/расходы в рапорте дня через фото чеков.

- [x] Строка «Dagens utlägg»: фотографируешь чек → он уходит в уже существующий скан (Claude vision) → подставляются поставщик и сумма → оба остаются редактируемыми (мятый чек — ровно то место, где скан ошибается) → сохранение создаёт расход на этот объект и эту дату с приложенным фото
- [x] В строке — бегущий итог расходов за день, чтобы цифра была видна пока день свежий, а не в конце месяца
- [x] Скан везде best-effort: выключен или упал — сумма просто набирается руками; не загрузился итог — рапорт всё равно работает
- [i] Отдельную сущность «расход материала за день» не заводил: чек и есть расход, а `expenses` уже связан с проектом, датой, статусом и согласованием
- 📦 `cf8374e7` · OTA `3f837d36`

## 14:55 · «Продолжай всё по списку» (ÄTA, фото, аттестация)

- [x] **Привязка дня к ÄTA**: строка появляется, только если у проекта вообще есть ÄTA — обычный договорной день остаётся коротким (Хик). Список ÄTA админский и содержит суммы, поэтому воркеру отдаётся узкий маршрут `GET /shifts/:id/ata-options` — только номер и заголовок, и только по СВОЕЙ смене
- [x] **Фото в рапорте дня** — через уже существовавшую загрузку фото на смену, счётчик рядом
- [x] **Аттестация**: у руководителя на завершённой смене строка «Attest» — тап подтверждает день, повторный снимает. Права переиспользуют тот же фильтр доступа, что и список смен, так что подтвердить чужую невидимую смену нельзя
- [x] Бэкенд: `ataId`, `approvedAt`, `approvedByUserId` на смене + `PATCH /shifts/:id/approval` — `70026a6`
- [x] 11 языков, 308 тестов зелёные
- 📦 `30082ed4` · OTA `00b85953`

## 14:40 · «Продолжай» (избранные проекты)

- [x] **Закрепление проектов**: свайп карточки ВПРАВО закрепляет — зеркальная пара к свайпу влево на удаление (как флаг в Mail). Закреплённые всплывают наверх списка и помечены звездой. Список проектов — это же то, что открывает фильтр по проекту, так что закрепление помогает и там
- [x] Хранится на устройстве, как раскладка главной, а не на аккаунте — это личная привычка, а не данные компании
- [x] `EntityListScreen` получил слот `leftAction` (свайп вправо) — теперь доступен любому списку
- [x] 4 новых юнит-теста на сортировку закреплённых, всего 308 зелёных
- 📦 `7c1359ea` · OTA `c3ca13d0`

## 14:25 · «Продолжай, да весь неиспользуемый код удаляй»

- [x] **«Kopiera <datum>»** в рапорте дня: одной кнопкой подставляет тип часа, километры, время в пути и суточные из прошлого отчётного дня. На одном объекте это изо дня в день одно и то же, а перенабор — ровно то, что люди перестают делать. Кнопка видна, только пока день не отчитан; если запрос не удался, экран просто открывается пустым (Zeigarnik — незакрытое дело давит, закрытие должно стоить один тап)
- [x] Бэкенд: `GET /shifts/last-report` — последний отчётный день воркера
- [x] **Удалён мёртвый код**: `time-report.service.js` (бил в несуществующий на бэкенде `/time-reports`) + два его экспорта, `byggexpWordmark.js` (никто не импортировал), и убраны лишние `export` у внутренних хелперов `paymentDue` и `ReminderSheet`
- [i] Прогнал knip: неиспользуемых ФАЙЛОВ больше нет (кроме `scripts/pwa-postexport.mjs` — это инструмент веб-сборки, запускается руками при деплое, оставил). Осталось ~40 неиспользуемых экспортов — часть используется внутри своего файла, часть держат тесты; чистить отдельным заходом с прогоном тестов, а не вслепую посреди рабочей сессии
- 📦 `ace5fc28` · OTA `5434a507`

## 14:10 · «Берём всё, что есть у них, только в нашем дизайне. И с учётом lawsofux.com»

**Твоя задача:** реализовать функциональность Next в нашем дизайне по законам UX.

**Разбор Next (project.next-tech.com, PWA, Version 4.0):** один быстрый рапорт за день (дата, проект, рабочий заказ/ÄTA, роль, часы) + дневник + фото + артикулы + отсутствия; копирование вчерашнего; избранные проекты; чек-листы; аттестация часов; SMS о незаполненных часах; выгрузка в зарплату. OB/övertid/traktamente/milersättning на их сайте НЕ описаны — проверил 3 страницы.

- [x] **Шаг 1 — «Dagens rapport»**: на своей завершённой смене воркер указывает тип часа (Normal / Övertid / OB — зарплата уже умеет множитель), поездку (км, время в пути, суточные) и заметку дня. Открывается с карточки смены рядом с ручными часами; карточка показывает, отчитан ли день
- [x] Бэкенд: поля на самой смене (смена И ЕСТЬ запись дня, отдельная коллекция пришлось бы джойнить обратно) + `PATCH /shifts/:id/report`, пишет только присланные поля, только своя смена — `d6fec20`
- [x] Законы UX применены осознанно: **Хик** — выбор строкой пилюль, а не выпадашкой; **Миллер** — три блока вместо стены полей; **Фиттс** — цели 44pt, кнопка сохранения в зоне большого пальца; **Якоб** — наши же карточки и inset-разделители, учиться нечему; **Постел** — «7,5» и «7.5» одинаково принимаются, пусто = 0
- [x] 11 языков
- 📦 `2f9e7d8c` · OTA `a05ce85f`

## 13:56 · «Цвета в Mitt arbete сделай как везде, не должно быть там красного» (скрин Home)

**Твоя задача:** убрать красный из карточки Mitt arbete.

- [x] Убрал `#FF3B30` и `#FF9500`: просроченные и «скоро» теперь тем же приглушённым цветом даты, что и остальные карточки главной. Срочность несёт сама формулировка («Försenad 8 aug.»), а не цвет, выбивающийся из стекла
- 📦 `33576bcd` · OTA `6593ba00`

## 13:53 · «Сделай такой же фильтр у оферт и фактур, только фильтровать частный клиент или фирма» (скрин Verktyg)

**Твоя задача:** дропдаун-фильтр по типу клиента на оферты/фактуры.

- [x] Новый общий `FilterSelector` — та же плашка с шевроном, что у фильтра по проекту, но выбор из простого списка в шторке. Триггер берёт стили ИЗ файла проектного фильтра, так что разъехаться они не могут
- [x] На оферты и фактуры добавлен фильтр **Alla / Företag / Privatpersoner** над пилюлями статусов
- [i] Как определяется тип: документы снимают имя и номер клиента, но НЕ его тип. Поэтому тип берётся из реестра клиентов — по `customerNumber` (есть у фактур), иначе по названию; ROT-фактура считается частным лицом по определению; неизвестный клиент → «фирма» (дефолт сервера)
- 📦 `9a8318d9` · OTA `b0b5cb4a`

## 13:40 · «Делай» (перевести остальные списки на общий компонент)

**Твоя задача:** все экраны-списки через один компонент.

- [x] На `EntityListScreen` переведены: **проекты, инструменты, задачи, оферты, фактуры** (клиенты и артикулы были раньше). Семь копий одной и той же оболочки → одна
- [x] В компонент добавлены слоты под то, что у экранов своё: `beforeList` (фильтр по проекту, пилюли статусов, поиск), `headerRight` (кнопка Skanna, реестры «…»), `sections` + `renderSectionHeader` (задачи сгруппированы по проектам), `listHeader` (строка «Alla projekt»), `onBack`/`onNavigateHome`/`onNavigateMenu`/`onAdd` (у проектов навигация под защитой выхода)
- [x] В каждом экране осталось только своё: данные, карточка и вызов удаления
- [x] Линт чистый, 303 юнит-теста зелёные
- 📦 `0cd84d72` · OTA `5091d56b`

## 13:26 · «Оферты и фактуры — уже две отдельные сущности, переключатель не нужен» → «Кнопка с главной ведёт на список, создание оттуда» → «Сделай один компонент, чтобы менять в одном месте»

**Твоя задача:** развести оферты и фактуры, кнопки на главной → списки, и общий компонент списка.

- [x] Убрал сегмент-переключатель Offerter/Fakturor. Теперь два маршрута `Offers` и `Invoices`, заголовок по сущности; старый `Economy` оставлен (на него ссылаются онбординг и «Mitt arbete») и по умолчанию открывает оферты
- [x] Кнопки Offert/Faktura на главной ведут в СПИСОК, а не сразу в форму — создание через «+», как у всех сущностей
- [x] Новый **`EntityListScreen`** — единственное место, где живёт раскладка списка сущности: шапка, чипы-фильтры, карточки, свайп-удаление, пустое состояние, «+» в баре. Клиенты и артикулы уже рендерятся через него, так что правка раскладки прилетает в оба сразу
- [i] Проекты / инструменты / задачи / оферты / фактуры пока на своих экранах — у них есть свои доп-контролы (Skanna, фильтр по проекту, реестры «…», группировка по проектам). Перевести их на общий компонент можно следующим заходом, скажи

## 13:20 · «Конечно, для всех делай такое удаление»

**Твоя задача:** свайп-удаление на всех списках.

- [x] Оферты и фактуры (Ekonomi) — свайп влево → «Ta bort», строка уходит после подтверждения сервером
- [x] Артикулы — то же
- [x] Задачи — то же, но только тем ролям, кто может управлять задачами; после удаления список перечитывается (задачи лежат и внутри проектов, и в личном списке — сплайсить две формы хуже, чем перезапросить)
- [x] В `task.service` и `article.service` не было метода `remove`, хотя эндпоинты на бэкенде есть — добавил
- [i] Теперь свайп-удаление единообразно на: проектах, инструментах, клиентах, оферти, фактурах, артикулах, задачах
- 📦 `a463f735` · OTA `faf81a30`

## 13:13 · «Сделай возможность удаления клиента, также смахивая» + «Не помню, что создавал оферты — откуда они взялись?»

**Твоя задача:** свайп-удаление клиента; разобраться с двумя одинаковыми офертами.

- [x] Свайп влево по карточке клиента → красная «Ta bort», та же плашка, что в проектах и инструментах; строка уходит только после подтверждения бэкендом. Добавил `clientService.remove` (эндпоинт был, метода в сервисе не было)
- [x] **Причина двух одинаковых оферт**: защита от двойного тапа держалась на стейте `saving`, а второй тап срабатывает РАНЬШЕ, чем React перерисует кнопку disabled → создавались два документа. В админке это уже чинили через `submittingRef`. Поставил такую же синхронную защиту на оферту и на фактуру, на обе кнопки (Spara utkast / Skapa & skicka)
- [i] Кто именно создал те две оферты, по коду не скажу — но одинаковая пара с одинаковой суммой и датой ровно так и получается при двойном нажатии
- 📦 `83906ebe` · OTA `c03855de`

## 13:05 · «Весь флоу бери из админки — по созданию клиента, артикула, фактуры и оферты, там всё уже точно»

**Твоя задача:** привести 4 формы к админке.

- [x] **Фактура — byggmoms**: `reverseVAT` был жёстко зашит в `"false"`, теперь берётся у клиента (как в `InvoiceForm`) и участвует в расчёте сумм. Это реальная ошибка для B2B-строительства
- [x] **Фактура — ROT**: добавил два поля, без которых вычет не заявить в Skatteverket — `rotPersonalNumber` (личный номер покупателя) и `rotProperty` (обозначение недвижимости / BRF). Раньше уходила только сумма работ
- [x] **Фактура — остальное из админки**: `ourReference` (по умолчанию = имя того, кто выставляет), `yourReference` (= контактное лицо клиента), `deliveryDate`, `paymentTerms`, `city`, `status: draft`
- [x] **Оффер**: `status: draft` + контактное лицо (роль + имя) — та же одна строка по умолчанию, что в `OfferForm`. Суммы бэкенд считает сам, поэтому их не шлём
- [x] **Артикулы**: экран открывался формой, артикулы были закопаны под ней → теперь список (как `ArticleListPage`), форма на отдельном экране `CreateArticle`, «+» в нижнем баре
- [i] `companyFooter` слать не нужно — бэкенд сам подставляет реквизиты компании (`resolveCompanyFooter`)
- 📦 `537e7a53` · OTA `92bff084`

## 12:53 · «С клиентами так же, как с проектами / работниками / инструментами — список, фильтр частные или фирмы наверху, по умолчанию все» (3 скрина)

**Твоя задача:** Klienter должен быть экраном-списком, а не формой.

- [x] Экран открывался формой создания, а сами клиенты были закопаны под ней. Теперь это список как Projekt / Anställda / Verktyg: карточки с бейджем типа (Företag / Privat), метаданные строкой (номер · email · телефон)
- [x] Фильтр наверху: **Alla** / Företag / Privatpersoner, по умолчанию Alla. Клиенты без типа считаются фирмой (так же по умолчанию на сервере)
- [x] Форма переехала на отдельный экран `CreateClient`, открывается синим «+» в нижнем баре; после сохранения возвращает в список, который перечитывается на фокусе
- [x] 11 языков
- 📦 `ce60084c` · OTA `a07f6607`

## 12:51 · «Проверь, если фирма, то блока с ROT не будет?» (скрин Ny faktura)

**Твоя задача:** проверить ROT для клиента-фирмы.

- [x] Была ошибка: блок ROT показывался ЛЮБОМУ клиенту, и `rotEnabled` мог уехать на фактуру фирмы. В админке (`InvoiceForm`) он показан только при `clientType === 'private'` и сбрасывается при смене клиента
- [x] Теперь так же: блок скрыт, если клиент не частное лицо; смена клиента на фирму гасит тумблер и сумму; в payload `rotEnabled` форсированно false для фирмы
- 📦 `9068bfde` · OTA `5a880c46`

## 12:49 · «Title заменить на Beskrivning» (скрин Ny faktura)

**Твоя задача:** подпись строки фактуры.

- [x] Экран оффера уже передавал «Beskrivning», а фактура падала в дефолт `itemTitle` = «Titel». Сделал «Beskrivning» дефолтом редактора строк — теперь совпадает на обоих экранах; неиспользуемый ключ `itemTitle` вычистил из 11 локалей
- 📦 `56bf70a4` · OTA `42b4ffce`

## 12:35 · «Заход 2 делай»

**Твоя задача:** колокольчик-напоминание на задаче и на заметке.

- [x] Общий `ReminderSheet`: выбор момента (inline-пикер на iOS, дата→время на Android) + для задачи пилюли повтора «Один раз / каждые 15 / 30 / час»
- [x] Задача: колокольчик в строке (TasksPreview). Пишет ровно тот же контракт, что попап в админке — выбранный момент становится дедлайном, интервал > 0 включает «напоминать до выполнения». Сброс сохраняет дедлайн, гасит только напоминание
- [x] Заметка: колокольчик в строке (NotesPreview) — одно напоминание, хранится в новом поле `remindAt` на сервере, а не локально, поэтому переживает перезапуск
- [x] Колокольчик синий, когда напоминание стоит
- [x] Бэкенд: `remindAt`/`remindedAt` у заметки + крон `note-reminders` (раз в минуту, чтобы «в 14:30» приходило вовремя); `remindedAt` ставится ДО отправки пуша, поэтому упавший пуш не превращается в ежеминутный спам; перенос/сброс колокольчика взводит заново — `4ea6a30`
- [x] 11 языков
- 📦 `f4243e9f` · OTA `2b172402`

## 12:15 · «Напоминание о входящих счетах и о заметках… предложи варианты» → «Если в админке Mitt arbete, то и тут так же. Действуй»

**Твоя задача:** заход 1 — счета, под именем «Mitt arbete» как в админке.

- [x] Новая секция главной **«Mitt arbete»**: неоплаченные входящие счета (срочные первыми, просроченные красным, ближайшие 7 дней оранжевым) + просроченные задачи. До 3 строк каждого
- [x] Тап по кружку у счёта = «оплачено» → статус `paid`, напоминания прекращаются
- [x] `paymentDue.js` — дословный порт из админки (`isUnpaid`/`daysUntilDue`/`paymentDueTone`, порог 7 дней): красное/оранжевое значит одно и то же в вебе и в приложении
- [x] Новый `supplier-invoice.service.js` (`GET /supplier-invoices`, `PATCH :id/status`) — в мобиле его не было
- [x] Секция ВЫКЛЮЧЕНА по умолчанию, включается в Anpassa startsidan; счета грузятся только при праве `finance.manage`
- [x] Бэкенд: крон `payment-reminders` — пуш админам за 7 дней, за 1 день, в день срока и раз в сутки пока просрочен; веха пишется в счёт, `setStatus` её сбрасывает (оплатил → тихо, переоткрыл → снова напоминает) — `f9053b9`
- [x] 11 языков
- 📦 `3269a114` · OTA `ef8b0c63`

## 12:07 · «Значок фактуры сделай по-другому — символом денег, монеты, из той же библиотеки» (скрин Home)

**Твоя задача:** иконка фактуры не отличается от оффера.

- [x] `file` → `dollar-sign`. Монет в Feather нет, денежный символ там один; альтернатива — `credit-card`, скажи если лучше
- 📦 `189218f5` · OTA `a157406a`

## 11:52 · «Сюда надо добавить ещё две кнопки — оферта и фактура, их создание; иконки из тех же коллекций, тот же принцип; также их можно добавлять и убирать с главного экрана» (скрин Home)

**Твоя задача:** две новые кнопки на главной — создание оффера и фактуры.

- [x] `offer` → экран CreateOffer, `invoice` → CreateInvoice; иконки Feather `file-text` / `file` — ровно те, что в меню у Offerter/Fakturor
- [x] По умолчанию ВЫКЛЮЧЕНЫ, включаются/выключаются в Anpassa startsidan как любая другая кнопка (drag-порядок тоже работает)
- [x] Новое поле кнопки `permission` (у этих — `finance.manage`): скрыты у того, у кого нет права, зато видны делегированному «офисному» юзеру без админ-роли
- [x] Подписи в 11 языках + цвета для colorful-темы (из существующей палитры, новых оттенков не выдумывал)
- 📦 `ed8d6043` · OTA `ff39e54d`

## 11:45 · «Именно этот юзер он не добавлял инструмент… поэтому мы не ставим» + «Про этот экран я тебе давал задачу сегодня» (2 скрина Kom igång)

**Твоя задача:** галочка ставится, только если ЭТОТ пользователь сам это сделал.

- [x] Каждый сигнал чеклиста теперь сверяется с текущим юзером: `createdByUserId` у инструментов / задач / клиентов / артикулов / офферов / фактур, `createdBy` у приглашённого сотрудника, owner-или-руководитель у проекта
- [x] Бэкенд: у инструмента не было автора — добавил `createdByUserId`, пишется при создании (`e0d9dca`)
- [x] Бэкенд: у компании не было «кто заполнил» — добавил `detailsUpdatedByUserId`, пишется при сохранении формы (`76d9a5d`); «Fyll i företagsuppgifter» отмечается только у того, кто её заполнил
- [i] У старых записей автора нет → шаг станет неотмеченным, пока юзер не сделает своё. Это и есть нужное поведение
- 📦 `450d852d` · OTA `c8e35532`

## 11:34 · «Убери этот. Описание ячейку сделай ниже/меньше» (скрин Skapa uppgift)

**Твоя задача:** убрать «Interna anteckningar», описание сделать ниже.

- [x] «Interna anteckningar» вырезаны целиком: строка, стейт `notes`, отправка поля в submit
- [x] Ячейка Beskrivning: minHeight 70 → 40, растёт по мере набора
- 📦 `deee5001` · OTA `1b5aae63`

## 11:33 · «Почему отмечены?» (скрин Kom igång, fieldwork)

**Твоя задача:** в чеклисте отмечены Lägg till verktyg и Anpassa startsidan.

- [i] «Lägg till verktyg» — честно: ты сегодня создал 4 инструмента, шаг засчитан верно
- [x] «Anpassa startsidan» — остаток старого поведения: флаг «открыл дровер» уже лежал в AsyncStorage с 08:53, фикс в 08:58 убрал только НОВЫЕ срабатывания. Версионировал ключ (`home-onboarding-customize-opened` → `home-onboarding-customized-v2`), старый флаг больше не читается и чистится при смене юзера; тест обновлён
- 📦 `a8903871` · OTA `f8f2852e`

## 11:20 · «Стиль сломался — смотри как у настроек разделительная линия. Заметки перенеси к заголовку. После идут напоминания. Тут уже приоритетность и повторение» (скрин Skapa uppgift)

**Твоя задача:** починить разделители и переставить блоки.

- [x] Разделители: строки больше не рисуют сплошной бордер во всю ширину. Карточка рисует hairline с отступом — 58 после иконки, 16 когда иконки нет. Те же линии, что в настройках/меню (`FieldRow.sepIcon/sepPlain`, `CreateTool.rowSep`)
- [x] Beskrivning + Anteckningar переехали в карточку с заголовком (описывают одно и то же)
- [x] Новый порядок: заголовок (+описание+заметки) → даты → **Aviseringar** → **Prioritet + Upprepa** → файлы
- 📦 `912a5e5a` · OTA `3b80b5f3`

## 11:17 · «Делай возможность удалять инструмент который админ сам добавил, также как и сотрудников — влево смахивая» (скрин Verktyg)

**Твоя задача:** удаление инструмента свайпом влево.

- [x] Свайп влево на карточке инструмента → красная «Ta bort» (тот же `Swipeable` + та же красная плашка, что в списке проектов). Без подтверждения — сам свайп и есть защита, как у проектов
- [x] Видно только ролям, которые управляют инструментами (superadmin / companyAdmin / projectAdmin) — бэкенд уже проверяет доступ и компанию на `DELETE /tools/:id`
- [x] Карточка пропадает только после подтверждения бэкендом; при ошибке — алерт с `[код] сообщение`
- [x] Ключи `common.delete` + `tools.deleteFailed` в 11 языках
- 📦 `d28b53e7` · OTA `c84bec3c`

## 11:01 · «дела как в админке» (2 скрина Create task из админки)

**Твоя задача:** сделать создание задачи в приложении как в админке.

- [x] Prioritet (Låg / Normal / Hög) и Upprepa (Upprepas inte / Varje dag / Varje vardag / Varje vecka / Varannan vecka / Varje månad) — отдельной карточкой, выбор через новый `OptionPickerModal`
- [x] Оба поля уходят на бэкенд — он их уже поддерживал (`priority`, `recurrence`), мобила НИКОГДА не слала → всё падало в normal/none
- [x] Даты предзаполнены: сегодня, старт 08:00, дедлайн 17:00 — дефолты админки
- [x] 11 языков
- [i] Påminnelser + «Meddela chefen efter N» в мобиле УЖЕ есть (шторка Notifications), просто разложено иначе, чем в вебе. Один-в-один выпадашка (Av/15/30/час/день) — по запросу
- 📦 `16cdb2ef` · OTA `e997233a`

## 10:01 · «Heldag убери вообще» → «Отовсюду»

**Твоя задача:** убрать блок Heldag из Skapa uppgift полностью.

- [x] Вырезал: строку-свитч, стейт `allDay` + поле в черновике, `buildAllDayRange` и мёртвые хелперы времени, стили `allDayTextContainer`/`allDayHint`, ключи `createTask.allDay*` во всех 11 локалях. Блок Schema = только Börjar / Slutar
- 📦 `787bca71` · OTA `5042b445`

## 08:53 · «Я еще не добавлял ни клиента ни фактуру ни своих данных не заполнял» + «Экран тоже не настраивал» (2 скрина чеклиста)

**Твоя задача:** в онбординге отмечены пункты, которых ты не делал.

- [x] «Anpassa startsidan» отмечался при ПРОСТОМ ОТКРЫТИИ дровера → теперь флаг ставится внутри дровера, когда реально меняешь тему/кнопку/секцию — `dc335160` · OTA `80e73987`
- [i] Остальные три (företagsuppgifter / klient / offert-faktura) — НЕ баг: все запросы строго companyId-scoped (проверил `findAccessible` у clients/offers/invoices), демо-сидов нет. Значит в компании реально лежат orgNumber + ≥1 клиент + ≥1 оффер/фактура (заведены раньше через админку). Хочешь «сделал ли это Я» вместо «есть ли у компании» — переделать сигнал на per-user (ЖДЁТ ТВОЕГО ОТВЕТА)

## 08:49–08:50 · «Создал инструмент а его и нету» + «Не показывает ни проекты ни людей» (3 скрина)

**Твоя задача:** инструмент создался, но списка нет; в «Koppla till projekt/arbetare» пусто.

- [x] ProjectAdmin в `findAccessible` видел ТОЛЬКО инструменты своих проектов, а у нового инструмента `projectIds` пуст → создатель не видел собственный инструмент. Теперь список скоупится по companyId как у CompanyAdmin (ProjectAdmin и так цепляет инструменты по всей компании) — бэкенд `530701f` (08:52, прод)
- [x] `GET /users/role/:role` = ТОЛЬКО superadmin → 403 для company/projectAdmin, и один `Promise.all` reject обнулял ЗАОДНО и проекты. Теперь `/users/my-company` + фильтр `role==='worker'`, загрузки независимы (`allSettled`) — `f90bbb2e` · OTA `80e73987`

## 08:48 · «Добавь функцию сканирования» → «У каждого инструмента свой QR-код и можно добавить просто отсканировав» (скрин Lägg till verktyg)

**Твоя задача:** перенести логику сканирования на экран создания инструмента.

- [x] Новая строка «QR-kod» → камера (переиспользуемый `QrScannerModal`, вынес из ToolScanScreen) → код сохраняется как `qrId` инструмента, потом «Skanna» по нему находит инструмент
- [x] Бэкенд принимает `qrId` на create (trim + upper-case, 409 если код занят), без кода — генерит сам как раньше — `1c85b4f`
- [x] 11 языков · OTA `80e73987`

## 08:40 · «Давай в 1м стиле все сделаем» → «Пока заметок нету так же в центр и с иконкой» (скрин Home)

**Твоя задача:** пустая карточка Anteckningar выбивается из стиля Dagsrapport/Uppgifter.

- [x] Пустой стейт по центру: иконка `edit-3` + «Skriv en anteckning…», карточка 130px как у соседей. Инпут остаётся смонтированным (свёрнут), тап разворачивает и фокусит
- 📦 `5d04f930` · OTA `3cd2996a`

## 08:36 · «Убери Avtal кнопку и из окна Ekonomi. В окне Ekonomi сделай строчками, разбей на 3 блока по 2 строчки» (3 скрина)

**Твоя задача:** убрать Avtal, Ekonomi строками, 3 блока по 2, строки как Projektnamn/Orderreferens но не обязательные.

- [x] Убрал кнопку «Avtal» на Skapa projekt + карточку AVTAL/Avtalsnr в окне Ekonomi; выпилил `ContractSection`, стейт `contractNumber` и его append в submit
- [x] Ekonomi: 2-колоночные пилюли → полноширинные СТРОКИ
- [x] 3 блока по 2 строки (budget/timmar · planerat/förbrukat material · självkostnad/debiteras)
- [x] Строки = новый `FloatingField` (floating label + hairline sep, безрамочный), без звёздочки
- [i] Заголовок «EKONOMI» внутри убрал — шапка экрана уже «Ekonomi» (дубль)
- 📦 `c3b30129` · OTA `e246b544`

## 08:15–08:22 · Организация работы

- [x] Свёл всё в один `WORKLOG.md` (был `TZ_TASKS.md`), `HANDOFF.md` помечен архивом — `54bd7e1f`
- [x] Добавил секцию «Очередь»: задачи выписываются ДО выполнения — `d2b36325`

# 2026-09-19 → 20, ночная сессия (раунды 1–5)

## Экран Projektadress (LocationPickerModal)

- [x] Кнопку сохранить — синяя галочка в шапке справа (как в Skapa projekt) — OTA
- [x] Выбор радиуса — над картой — OTA
- [x] Скрывать клавиатуру при выборе адреса / касании слайдера / карты — OTA
- [x] Слайдер радиуса не тянулся на Android (убрал сломавший wrapper, вернул прямой Slider) — OTA — ⏳ проверить перетаскивание на устройстве
- [x] Слайдер пропал → восстановил — OTA
- [x] Бегунок (thumb) не виден → белый thumb — OTA (виден на 6259)
- [x] Галочка на разной ВЫСОТЕ (модалка без safe-area) → paddingTop insets.top — OTA
- [x] Галочка РАЗНОГО ЦВЕТА на разных экранах → HeaderCheckButton теперь = theme.colors.primary (как FAB), убрал disabled-затухание на Projektadress — OTA

## Экран Skapa projekt (CreateProject)

- [x] Notes: не виден placeholder (тёмная тема) → placeholderTextColor — OTA
- [x] Notes: «чёрная полоса» = Android underline → underlineColorAndroid="transparent" — OTA
- [x] Notes: ячейку чуть меньше → minHeight 100→64 — OTA
- [x] Имя проекта: при клике авто-адрес очищается — OTA

## Меню / профиль

- [x] У АДМИНА в шапке меню «Sverige Bygg AB» → у companyAdmin имя-строка = РОЛЬ (Företagsadministratör), бейдж прячется если нет профессии — OTA
- [x] Home-счётчики «сверяются с БД» (рефетч на фокусе) — OTA
- [x] Тёмное меню (dark theme) — OTA (раунд 1)
- [x] Бейдж профессии вместо «Arbetare» у воркера — OTA (раунд 1)
- [x] Онбординг: шаг «Anpassa startsidan» админу — OTA

## Разъяснения (НЕ баги кода)

- [i] «Sverige Bygg AB» нигде не захардкожено (грепнул 3 репо) — это `user.name` аккаунта в БД
- [i] Оффер/фактура/клиент в чеклисте отмечены — запросы строго companyId-scoped; данные реально есть на аккаунте `company@byggexp.se`
- [i] «OTA не долетела до Android» — долетела (билд 26 = runtime 1.1.0/production); применяется на перезапуск
- [i] iOS геозона: realtime-push при выходе не приходит — ограничение iOS region-monitoring (foreground safety-net ловит на возврате); фикс = syslog-диагностика / больший радиус (трогает закреплённый конфиг) — открыто

## Раунд 3 (фидбэк 21:20+)

- [x] Онбординг «Bjud in ditt team» не отмечался после добавления сотрудника → `hasTeam` теперь = есть любой юзер компании кроме админа (вкл. приглашённых pending), без требования заполнять все поля — OTA
- [x] Слайдер thumb — белый, чуть выше (transform-скейл откатил: делал овал/клип); реально БОЛЬШЕ thumb = нужен `thumbImage`-ассет (могу добавить)
- [ ] Чёрная линия у notes — в КОДЕ источника нет (ни бордера, ни divider, ни глоб.дефолта); `underlineColorAndroid="transparent"` НЕ срабатывает на Android New-Arch (Fabric) для multiline; OTA с фиксом уже применён, линия осталась → нужен Android ПО КАБЕЛЮ (adb dump дерева вьюх), вслепую не найти

## Раунд 4 (adb-сессия, подтверждено на устройстве)

- [x] Чёрная линия notes — источник = нативный EditText underline (нашёл adb-дампом); фикс = сплошной backgroundColor — ✅ OTA
- [x] Слайдер радиуса **тянется на Android** — заменил @react-native-community/slider на свой `RadiusSlider` (PanResponder, сам захватывает касание) — ✅ ПОДТВЕРЖДЕНО Alexander'ом
- [x] Thumb крупный (44dp) + круговая тень (offset 0,0) — ✅
- [x] Меню: у админа имя (`user.name`) + бейдж профессии как у воркера (откат «роль вместо имени») — ✅ OTA
- [x] Ekonomi (`FieldInput`): крупнее/серая подпись + выровненные 2-колоночные пилюли — ✅ OTA
- [x] `/app/magic` fallback: кнопка + авто «Öppna appen» (deep-link), иначе стор — ✅ backend

## Раунд 5 (фидбэк 23:xx, iPhone по кабелю)

- [x] Удаление проекта: ProjectAdmin получал `[403] Access denied` → бэкенд: добавил ProjectAdmin в `@Roles` на `DELETE /projects/:id` + `assertCanDeleteProject` (ProjectAdmin удаляет только свои: owner/projectManager) — ✅ прод `89280b9` (серверный, без пересборки/OTA)
- [x] Диагностика удаления: on-screen Alert показывает `[status] message` — сработало (скриншот `[403]`). NB: `idevicesyslog` на iOS 26 мёртв (Apple сменила протокол c iOS 17+), релиз режет console.* — логи по кабелю НЕ работают, юзать on-screen диагностику
- [x] Orderreferens: перенёс в ту же карточку, что Projektnamn, строкой под ним, без рамки (floating label + hairline sep) — ✅ OTA `bd0c4e8e`
- [x] Ekonomi «цвет как на создании»: замерил реальные пиксели — лейблы уже были идентичны (#7F7F7F=#000@50%). По выбору Alexander (AskUserQuestion, выбрал ВСЕ 3): лейблы → чёрные, поля → безрамочные, заголовки EKONOMI/AVTAL → чёрные. Через новые пропсы FieldInput `borderless`/`labelStyle` (shared-компонент не сломан) — ✅ OTA `60e55866`
- [x] Anteckning: отступ снизу 28→20 = как у всех блоков (сверху/снизу одинаково) — ✅ OTA `60e55866`
- [i] Процесс: при «сделай цвет как X» — СНАЧАЛА замерять пиксели+код, не на глаз; сохранено в память `feedback_color_match_sample_pixels`

# Открытые / след. шаги

_Обновлено 2026-09-21, 13:00. Отсюда продолжать в следующий раз — сверху вниз._

## 0. Первым делом в следующий раз

- [ ] Спросить Alexander, что показал вечерний тест GPS (смена вставала на паузу при выходе с объекта?)
- [ ] Проверить на устройстве свежий OTA `66b10e89`: уведомление геолокации НЕ всплывает при открытии приложения; уведомление про неоплаченный счёт открывает экран Leverantörsfakturor на нужном счёте; «•••» на карточке фактуры удобно нажимается
- [ ] Дописать в этот файл всё, что делаем, СРАЗУ после каждого OTA (сегодня копил пачкой — Alexander заметил)

## 1. Релиз 1.1.2 (ближайшее)

- [x] Билды **iOS 188** и **Android 27** собрались (FINISHED)
- [ ] Отправить: `npx eas submit -p ios --latest` и `npx eas submit -p android --latest`
- [ ] В App Store Connect заполнить «Что нового» (черновик не писан — попросить; по-шведски, про тёмную тему по Figma, онбординг с двумя вопросами, артикулы/скидки/НДС в фактурах)
- [ ] Обновить описания в Play Console и App Store Connect по `docs/STORE_LISTING_COPY.md` (в Play описание уходит на ревью вместе с релизом)
- [ ] Проверить процент раскатки в Play Console → Production → rollout (публичная страница этого не показывает)

## 2. GPS — Alexander тестировал 2026-09-20 вечером

- [ ] Забрать у него результат теста; если смена не встала на паузу при выходе — смотреть `GET /shifts/:id/timeline` (check-in / geofence-exit / return / checkout)
- [ ] iOS: realtime-push при выходе из геозоны — ограничение iOS region-monitoring, решения нет (больший радиус трогает закреплённый gps-конфиг; syslog по кабелю на iOS 26 без pymobiledevice3+tunnel недоступен)
- [ ] Android: слайдер радиуса — перетаскивание подтверждено, проверить, что не регрессировало на свежем билде

## 3. Дизайн: досверить цветную тему с Figma

- [ ] Цвета сверены и исправлены (см. 20:40). Осталась **геометрия**: высота карточки, радиус, зазоры сетки, кегль подписи. Figma отдавал `429` — повторить запрос: файл `vXrFV6ymlOokXGiZCQP6NE`, страница «Главные» (`187:2883`), фрейм «iPhone 14 & 15 Pro – 57» / `Frame 5870` (сетка 341×427)
- [ ] Токен для Figma одноразовый, на сессию — просить у Alexander заново
- [ ] Правило, которое он повторил дважды: **правки цвета/типографики делать только в той теме, о которой речь**; выносить в токены темы (`labelSize`, `labelFamily`, `textWeight`, `infoBadgeAlpha`), а не в общий `common`

## 3a. Открытые вопросы к Alexander

- [x] ~~Наследовать ли галочки чеклиста~~ — решено 22:08: клиент / артикул / документ наследуются фирмой, задачи и инструменты остаются персональными
- [x] ~~Одно правило отступов текста~~ — применено 22:12 (желоб 20 / карточка 20 / текст на 40)
- [x] ~~Уведомление «Shift location active»~~ — ресёрч сделан (22:20), текст переведён (22:08), лишний перезапуск убран (22:39), так что при каждом запуске оно больше не всплывает
- [ ] **Решить по геофенсингу на Android**: (а) оставить как есть — уведомление висит, пока идёт смена, канал `IMPORTANCE_LOW`, пользователь может выключить его отдельно; (б) перепроверить Play-Services Geofencing под флагом — если регистрация пройдёт, FGS и уведомление можно убрать совсем, ценой задержки 2–6 мин. В августе регистрация отклонялась: «Geofencer: registration not permitted» (коммит `ca0f2832`)

## 4. Фактуры/оферты — что осталось за бортом

- [ ] **Редактирование существующей фактуры/оферты** с телефона: сейчас только создание + действия из списка (отправить / скачать / оплачено). Правка строк существующего документа — только в админке
- [ ] Контактных лиц в оферте на мобильном **одно**, в админке список — если понадобится, добавить «+ контакт»
- [ ] Кредит-нота / частичная оплата — только в админке
- [ ] **Входящие счета (новый экран 22:46)**: пока только просмотр + «оплачено». Нет создания/загрузки счёта с телефона и не показаны вложения (бэкенд умеет: `POST /supplier-invoices`, `/:id/attachment`)
- [ ] У артикула нет цены ни в приложении, ни в админке (`priceExclMoms` всегда 0) — цена ставится в строке документа. Если надо, чтобы артикул нёс цену, это правка ОБОИХ форм + бэкенда

## 5. Старое, не срочное

- [ ] Ekonomi: чёрные безрамочные стили НЕ применены к экрану деталей проекта (`ProjectScreen.jsx:706/714`, `ecoSectionTitle`)
- [ ] ~40 неиспользуемых экспортов по knip (тема/геозона/расписание) — чистить отдельным проходом с тестами
- [ ] Исторические смены с 25 ч в сутках (16 + 9) правятся вручную — правило «смены не пересекаются» не ретроактивно

---

# Служебное (мои правила ведения файла)

## 📥 Очередь (capture ПЕРЕД работой)

- [x] 19:09 Шрифт заголовков карточек на главной (Anställda/Arbetspass/Projekt/Uppgifter) — как у заголовков блоков снизу (Dagsrapport), не жирно-чёрный
- [x] 19:10 Стрелка в селекторе проекта — белая
- [x] 19:11 Нижний бар — стиль как у карточек, без стекла/блюра
- [x] 19:14 Выдвижное меню Anpassa — УМЕНЬШИТЬ элементы (поправка 19:15)

> Как только Alexander кидает задачи (текст/скриншоты) — СНАЧАЛА выписываю каждую сюда как `[ ]`, потом делаю. В конце сессии тут не должно остаться `[ ]` без пометки почему отложено.

- (пусто — список Next закрыт, очередь чиста на 2026-09-20 15:10)
  - [i] У нас УЖЕ есть и лучше: GPS-смены с авто-паузой, напоминания о незаполненных часах (пуш + эскалация), отсутствия (semester/sjuk/VAB), расходы со сканом чека, дневник, чек-листы, ÄTA, PWA
  - [i] Мёртвый `time-report.service.js` удалён (бил в несуществующий `/time-reports`)
  - [ ] Оставшийся мусор по knip: ~40 неиспользуемых ЭКСПОРТОВ (не файлов) — темы/токены, гео-константы, хелперы расписания. Часть используется внутри своего же файла и просто не должна экспортироваться, часть держат тесты. Чистить отдельным заходом с прогоном тестов, не вслепую

## Правила

> 1. Новые задачи Alexander СНАЧАЛА пишу в «Очередь» как `[ ]`, потом делаю, отмечаю `[x]`, в конце сверяюсь.
> 2. Делаю ВСЕ пункты за один заход; нерешённый — в конец, но не стопорит остальные.
> 3. У КАЖДОГО пункта — дата и время (`2026-09-20 10:01`).
> 4. Пишу ЗАДАЧУ словами Alexander'а («Твоя задача:»), а под ней — что сделано. Новое — СВЕРХУ.
> 5. У каждого дня СНАЧАЛА таблица «Время | Что» (одна строка на задачу), ниже — подробности. В таблице САМАЯ СВЕЖАЯ задача — ПЕРВОЙ СТРОКОЙ (новое сверху, как и блоки).
> 6. Служебное (правила, очередь) держу В КОНЦЕ файла — сверху только то, что сделано.

> `HANDOFF.md` = архив прошлых сессий (не веду активно). `docs/*` = релизные/тех-доки.
