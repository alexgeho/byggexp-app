import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  timer: {
    alignItems: "center",
  },

  timerText: {
    fontSize: 140,
    fontFamily: "Landasans-Medium",
    letterSpacing: -2.5,
    color: "#FFFFFF",
  },

  timerTextSeconds: {
    opacity: 0.35,
  },
});